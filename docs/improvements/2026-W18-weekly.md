---
analysis_period: 2026-04-24 ~ 2026-05-01
mode: weekly
sample_size: 1 (주간 매도건) / 39 (트레일링 보장 발동 건수, 5영업일)
generated_at: 2026-05-01 23:50 KST
---

# 2026년 18주차 주간 거래 개선 제안서 (Phase A 1주 관찰 + 시스템 무결성 점검)

## 1. 분석 개요

- 분석 기간: **2026-04-24(금) ~ 2026-05-01(금)** — 5영업일 (4/24, 4/27, 4/28, 4/29, 4/30, 5/1 *5/1은 KRX 휴장 가능: 근로자의날, daily_snapshots에 sell/buy 0건 기록*)
- **주간 신규 매도 건수**: **1건 (한화오션, 4/27 보유기간 도달, +2.24%)**
- **AI 분석(`trade_reviews`) 적재 건수: 0건** ← 한화오션 매도 누락 (시스템 이슈 의심, 섹션 4 발견 1)
- AI 분석 완료 + JSON 파싱 가능 건수 (전 기간): N=14 (3월 후반~4/23) — Phase A 시기 직전까지의 표본
- 대상 전략: theme_momentum + 트레일링 + 분할익절
- **적용된 최신 파라미터 변경 (change_log.md)**:
  - 2026-04-24 — RSI 동적(BULL75/NORMAL70/BEAR65) + 테마 슬롯 보장(THEME_MIN_SLOT, FLOOR=25) 동시 배포 (Phase A) — DB v14
  - 본 주간이 **Phase A 1주 관찰 사이클의 종료 시점**

> ⚠️ **표본 부족 경고**: 주간 신규 매도건이 1건뿐이라 "성과 변화" 측정용 표본은 미달(N<5). 본 제안서는 **Phase A 1주 관찰 결과 + 시스템 무결성 이슈** 중심으로 작성하고, 손절/트레일링/보유기간 파라미터 조정 제안은 **유보**한다.

---

## 2. Before/After 추적 (Phase A 1주 관찰 결과)

### 변경: RSI 동적 + 테마 슬롯 보장 (2026-04-24 배포, change_log 행 21)

#### 2-1. 통과율 변화 (배포 전 4/21~4/23 vs 배포 후 4/24~5/1)

```sql
SELECT date, total_screened, total_passed, slot_protected,
       ROUND(100.0*total_passed/total_screened,1) AS pass_rate
FROM (...) -- 위 쿼리 참조
```

| 날짜 | 총 스크리닝 | 통과 | 슬롯 보장 | 통과율 |
|------|----------|------|---------|--------|
| 2026-04-21 (배포 전) | 78 | 12 | 0 | **15.4%** |
| 2026-04-22 (배포 전) | 78 | 21 | 0 | 26.9% |
| 2026-04-23 (배포 전) | 78 | 9 | 0 | **11.5%** ← 위기 시점 |
| 2026-04-24 (배포일) | 78 | 12 | 0 | 15.4% |
| 2026-04-27 | 78 | 12 | **5** | 15.4% |
| 2026-04-28 | 81 | 24 | **5** | 29.6% |
| 2026-04-29 | 80 | 21 | **5** | 26.3% |
| 2026-04-30 | 80 | 13 | **5** | 16.3% |
| 2026-05-01 | 79 | 12 | **4** | 15.2% |

**관찰**:
- 배포 전 3일 평균 통과율 17.9% → 배포 후 5일 평균 **20.6%** (+2.7%p)
- **테마 슬롯 보장 발동: 5영업일 중 4일 5/5 테마, 1일 4/5 테마 → 누적 24/25 = 96% 발동**
- 매수 실패일(buy_count=0): 5/1만 (이는 KRX 근로자의날 휴장 가능성, 섹션 9 메타 참조)

#### 2-2. RSI 동적 — 강세장 75 컷 발동 여부

```sql
SELECT date, theme, ROUND(MAX(rsi_at_screen),1) AS max_rsi
FROM screening_log
WHERE date >= '2026-04-27' AND rsi_at_screen IS NOT NULL
GROUP BY date, theme
ORDER BY date, max_rsi DESC;
```

전체 5영업일 KOSPI 등락률 흐름은 미수집(KOSPI 인덱스 별도 테이블 없음)이지만 RSI 분포로 간접 추정:
- 4/28 AI반도체 평균 RSI **76.6** (max 91.3) → **강세장(75 컷)이었다면 추가 다수 통과**
- 4/30 전기차 평균 RSI **75.4** (max 100) → 평시 70 컷이면 다수 탈락 예상
- 5/1 전기차 평균 RSI **77.8** (max 100) → 통과 0건 (slot_protected=0) ← **슬롯 보장 1건 발동 실패**
- 그러나 본 메모리(`project_buy_filter_phase_a.md`)에 따르면 4/24 KOSPI 등락률 +0.90%로 NORMAL 폴백 — 강세장 75 컷이 실제 발동하지 않은 날도 다수 있을 가능성

#### 2-3. 슬롯 보장 통과 종목의 최저 점수 분포

| 날짜 | 테마 | 보장 통과 종목 | 점수 | RSI |
|------|-----|------------|-----|-----|
| 4/27 | CXL | 퀄리타스반도체 | 31.7 | 67.0 |
| 4/27 | 아이폰 | LG디스플레이 | 32.2 | 60.8 |
| 4/27 | 통신 | 자람테크놀로지 | 34.4 | 61.1 |
| 4/28 | 건설 | IPARK현대산업개발 | **33.1** | 64.7 |
| 4/30 | 건설 | IPARK현대산업개발 | **30.1** ← 최저 | 59.0 |
| 4/30 | 전기차 | LG전자 | 33.3 | 69.6 |
| 5/1 | 건설 | 삼성물산 | 34.6 | 47.7 |

— 슬롯 보장 통과 종목들이 **최저 30~35점대**로 진입. THEME_SAFETY_FLOOR=25 기준으로는 정상.

#### 2-4. 결론 (Phase A)

| 지표 | 배포 전 (3일) | 배포 후 (5일) | 평가 |
|------|------------|------------|-----|
| 평균 통과율 | 17.9% | 20.6% | 개선 |
| 매수 실패일 | 1/3 (4/23 0건) | 0/4 영업일 (5/1 제외) | **개선** |
| 5테마 모두 ≥1 통과 | 0/3 | 4/5 | **개선 (슬롯 보장 효과)** |
| 슬롯 보장 발동 | N/A | 24/25 = 96% | 매우 활발 |

**종합 평가**: Phase A는 **초기 목표(매수 실패일 감소, 5테마 모두 진출 보장) 달성**. 다만 다음 두 우려 사항 존재:
- (a) 슬롯 보장 통과 종목 중 **최저 30점 케이스** 다수 — 향후 트레일링/손절 성과로 검증 필요(현재 표본 부족)
- (b) 5/1 전기차 테마는 평균 RSI 77.8로 **슬롯 발동 0건** — RSI 너무 과열 시 슬롯도 무력화됨

**롤백 트리거 점검**:
- 매수 실패일 ≤ 1/5: ✅ (실제 0/5; 5/1 휴장은 시스템 이슈 아님)
- RSI 70~75 통과 종목 5일 평균 수익률 ≥ -5%: **표본 0** (Phase A 매수 종목들이 아직 매도 미발생) → **판단 유보**
- 동일 테마 손절 2건 이상 동시 발생: ✅ 미발생
- **결론**: **롤백 트리거 미발동, Phase A 유지 권고**

---

## 3. 성과 요약 (주간 신규 매도건 기준)

| 지표 | 값 | 비고 |
|-----|----|------|
| 주간 매도 건수 | **1건** | 한화오션 4/27 (보유기간 도달) |
| 매도 평균 수익률 | +2.24% | (단일 표본) |
| trade_reviews 적재 | **0건** | ← 시스템 누락 (발견 1) |
| AI 분석 완료 | 0건 | 동상 |
| 보유 포지션 | 5개 (5/1 종가 기준) | LG디스플레이 -4.91%, 삼성전자 -2.74%, 에이디테크놀로지 -5.42%, DL이앤씨 +0.20%, 네패스아크 +3.69% |
| 누적 수익률 (cum_ret) | +7.66% (4/30 기준) | 4/24 +9.98% → 4/30 +7.66% (-2.32%p 회귀) |
| 주간 MDD | -2.11% | 4/30 |

```sql
-- daily_snapshots 주간 흐름
SELECT date, num_positions, buy_count, sell_count,
       ROUND(daily_return,3) AS daily_pct, ROUND(cumulative_return,3) AS cum_pct,
       ROUND(realized_pnl_today,0) AS today_pnl, ROUND(unrealized_pnl,0) AS unreal_pnl, ROUND(mdd,3) AS mdd
FROM daily_snapshots WHERE date >= '2026-04-24' ORDER BY date;
```
| 날짜 | 포지션 | 매수/매도 | 일간% | 누적% | 실현 | 미실현 | MDD |
|-----|------|-------|------|------|-----|-------|-----|
| 4/24 | 2 | 1/0 | 0.064 | 9.977 | 0 | +60240 | 0.000 |
| 4/27 | 1 | 0/1 | -0.510 | 9.417 | +34800 | -24480 | -0.510 |
| 4/28 | 2 | 1/0 | -0.194 | 9.204 | 0 | -43420 | -0.703 |
| 4/29 | 4 | 2/0 | -0.046 | 9.154 | 0 | -47920 | -0.749 |
| 4/30 | 5 | 1/0 | -1.369 | 7.660 | 0 | -180970 | -2.107 |
| 5/1 | 5 | 0/0 | 0.000 | 7.660 | 0 | -180970 | -2.107 |

**해석**: 미실현손익이 4/24 +60K → 4/30 -181K로 240K 약세 전환. 4/29~4/30 신규 진입 3건(에이디테크놀로지, DL이앤씨, 삼성전자)이 즉시 약세권 진입. 5/1 휴장으로 추가 변동 없음.

---

## 4. 핵심 발견

### 발견 1: **trade_reviews 적재 누락 — 4/27 한화오션 보유기간 매도 (시스템 이슈, 시급)**

- **증거 데이터** (쿼리 원문):
  ```sql
  -- trades 테이블에는 매도 기록 존재
  SELECT * FROM trades WHERE date='2026-04-27' AND action='sell';
  -- 결과: id=101, 한화오션, profit_rate=2.24%, reason='최대 보유 기간'

  -- 그러나 trade_reviews에는 누락
  SELECT * FROM trade_reviews WHERE stock_code='042660' OR sell_date='2026-04-27';
  -- 결과: 0건

  -- trade_reviews 최신 id, 최신 trade_id
  SELECT MAX(id) FROM trade_reviews; -- 40 (4/23 마지막)
  SELECT trade_id FROM trade_reviews ORDER BY id DESC LIMIT 1; -- 99 (4/23)
  ```

- **해석**:
  1. 한화오션 4/27 매도는 `trades` 테이블엔 정상 기록됨 (trade_id=101)
  2. `daily_snapshots`에도 4/27 today_pnl=34800, sell_count=1로 정상 반영됨
  3. 그러나 **`trade_reviews` 테이블에 해당 trade_id=101에 대한 행이 생성되지 않음**
  4. `database.py`의 `_save_partial_sell_to_db` 또는 `_close_position_in_db`에서 자동으로 `trade_reviews` 행이 생성되어야 하나, **`run_hold_period_sells()` 경로에서 누락된 것으로 보임**
  5. 영향: AI 사후 분석 미실행 → 보유기간 매도 케이스의 D+5 추적 데이터(post_trade_prices) 미생성 → `hold_days_review` 데이터 축적 지연

- **관련 코드 경로 추정** (검증 필요, 본 에이전트는 코드 수정 안 함):
  - `main.py:run_hold_period_sells()` → `trading_engine.execute_sell_orders()` → 매도 후 `trade_reviews` 적재 누락
  - 다른 매도 경로(트레일링/손절)는 정상적으로 적재되었으므로(`id=33~40` 정상), `run_hold_period_sells` 전용 경로 의심

- **제안 여부**: → **섹션 5 제안 1 (시급, 코드 조사)**

---

### 발견 2: **Phase A 매수 필터 효과 — 통과율 +2.7%p, 5테마 슬롯 보장 96% 발동**

- **증거 데이터**:
  ```sql
  SELECT date, total_screened, total_passed, slot_protected,
         ROUND(100.0*total_passed/total_screened,1) AS pass_rate
  FROM (SELECT date, COUNT(*) AS total_screened,
               SUM(CASE WHEN passed=1 THEN 1 ELSE 0 END) AS total_passed,
               SUM(CASE WHEN theme_slot_protected=1 THEN 1 ELSE 0 END) AS slot_protected
        FROM screening_log
        WHERE date >= '2026-04-21' GROUP BY date)
  ORDER BY date;
  ```
  결과는 섹션 2-1 표 참조. 평균 통과율 17.9% → 20.6%, 슬롯 보장 24/25.

- **해석**:
  - Phase A 배포의 **명시적 목표**(매수 실패일 ≤1/5, 5테마 진출 보장)는 달성
  - 5/1 전기차 테마(평균 RSI 77.8, max 100) → 슬롯 보장 발동 실패는 **RSI 컷이 슬롯 보장보다 우선**임을 보여주는 사례. **THEME_SAFETY_FLOOR(25점)** 미만이거나 RSI가 BULL 75 컷도 못 넘는 경우의 fallback 부재
  - 슬롯 보장으로 통과한 종목 최저 30점 (4/30 IPARK현대산업개발) — 매도 후 성과 확인 필요(아직 매도 발생 안 함)

- **관련 lesson_learned 인용** (Phase A 배포 직전 4/22 LG이노텍, 강한 모멘텀에서 트레일링 너무 빠른 매도 후 D+5에 +22.4% 추가 상승):
  > "테마주 급등 시 단기 조정을 모멘텀 약화로 오인하지 말 것. 거래량과 시장 분위기가 강할 때는 트레일링 스탑 여유를 더 두어 큰 상승파동을 완전히 포착해야 함" (011070, 2026-04-22)
  > "강한 테마 모멘텀 발생 시 단기간 고수익률 달성해도 섣부른 전량 매도보다는 분할 매도로 추가 상승 기회 포착 필요" (011070, 2026-04-22)

- **제안 여부**: → **섹션 5 제안 2 (관찰 연장, 1주 추가 후 재평가)**

---

### 발견 3: **트레일링 너무 빠른 매도 — D+5 큰 상승 케이스 (LG이노텍, 4/22 매도 4건)**

- **증거 데이터**:
  ```sql
  SELECT tr.stock_code, tr.sell_date, ROUND(tr.profit_rate,2) AS sell_pct,
         ptp.days_after_sell, ROUND(ptp.change_from_sell,2) AS chg_pct
  FROM trade_reviews tr JOIN post_trade_prices ptp ON ptp.review_id=tr.id
  WHERE tr.sell_date='2026-04-22' AND tr.stock_code='011070'
    AND ptp.days_after_sell IN (1,2,3,5);
  ```
  | 매도 | sell% | D+1 | D+2 | D+3 | D+5 |
  |-----|-----|-----|-----|-----|-----|
  | 1차 분할 (TP1) | +20.72 | +3.41 | +12.10 | +10.86 | **+22.44** |
  | 2차 분할 (TP2) | +20.47 | +3.63 | +12.33 | +11.09 | **+22.69** |
  | 3차 분할 (TP3) | +20.47 | +3.63 | +12.33 | +11.09 | **+22.69** |
  | 트레일링L2 | +19.10 | +4.82 | +13.63 | +12.37 | **+24.11** |

- **해석**: LG이노텍은 매도 후 D+5에 **추가로 +22~24% 상승**. 본 종목은 이미 본 주차 직전(4/22) 매도지만, AI(timing_score=3, Poor) 평가 일치. **현재 트레일링 L2(고점-3%)가 강한 모멘텀에 너무 민감**.
- 다만 이 발견은 본 주간(4/24~5/1)이 아닌 직전 주차(W17) 데이터이며, **이미 4/21 monthly 제안서에서 동일 패턴 발견**됨. **신규 가설이 아니므로 본 사이클에서는 정보 공유만**.

- **관련 parameter_suggestion 인용**:
  > "트레일링L2(약 3.75% 하락 시 매도)는 변동성 큰 테마주에 과도하게 민감. 아이폰 테마와 같은 강한 모멘텀 종목은 트레일링L3-L4(5-7% 하락 허용) 또는 트레일링 스탑 비율을 8-10%로 확대 권장" (011070, 2026-04-22)
  > "강한 모멘텀 섹터는 트레일링 레벨을 3-4단계로 상향 조정 필요" (011070, 2026-04-22)

- **제안 여부**: → **정보 공유만**. 본 주간 표본은 4/22 직전 데이터라 단독으로 트레일링 변경 근거 부족. **2026-04-monthly 제안서**에 이미 유사 가설 등재됨.

---

### 발견 4: **평시 손절 표본 부족 — Phase B(stop_loss) 재착수 트리거 미충족**

- **증거 데이터**:
  ```sql
  SELECT COUNT(*) AS peacetime_stop_loss_count, MIN(sell_date), MAX(sell_date)
  FROM trade_reviews
  WHERE sell_reason='손절' AND sell_date >= '2026-04-09';
  ```
  결과: **2건** (HPSP 4/13 -9.58%, 키움증권 4/20 -7.72%)

- **휴전(2026-04-09) 이후 평시 손절 매매 D+5 추적**:
  | 종목 | 매도일 | sell% | D+1 | D+2 | D+3 | D+5 |
  |------|------|------|-----|-----|-----|-----|
  | HPSP (403870) | 4/13 | -9.58 | +3.90 | +3.65 | +7.55 | **+11.33** |
  | 키움증권 (039490) | 4/20 | -7.72 | +2.15 | +0.45 | +1.02 | +2.49 |

- **해석**:
  - `memory/project_stop_loss_review.md` 트리거 #1: **평시 손절 ≥10건** → **현재 2건**, 미달
  - HPSP는 D+5 +11.33% 반등 → "조기 손절" 패턴 일치 (timing_score 2, Poor)
  - 키움증권은 D+5 +2.49% 소폭 → "적절 손절" 패턴 (timing_score 8, Good)
  - **단 2건의 표본 + 정반대 결론** → 어떤 방향으로도 의사결정 불가
  - `memory/project_stop_loss_review.md`에 명시된 **5/1 재평가 액션은 표본 미충족으로 자동 연기** 권고

- **제안 여부**: → **섹션 6 미결 항목 결론 (평시 손절 데이터 추가 축적 필요, Phase B 연기 유지)**

---

### 발견 5: **테마 카테고리 다양성 — 5테마 중 반도체 2개 동시 보유 (집중 위험 시그널)**

- **증거 데이터**:
  ```sql
  SELECT date, theme_name, ROUND(score,1) AS score, category, selected
  FROM themes WHERE date >= '2026-04-28' AND selected=1
  ORDER BY date DESC, score DESC;
  ```
  | 날짜 | 테마 | 점수 | 카테고리 |
  |-----|-----|-----|--------|
  | 5/1 | 전력반도체 | 42.1 | **반도체** |
  | 5/1 | 플랫폼 | 38.5 | IT서비스 |
  | 5/1 | 전기차 | 36.7 | 기타 |
  | 5/1 | 건설 | 35.6 | 산업재 |
  | 5/1 | AI반도체 | 34.4 | **반도체** ← 중복 |

- **해석**:
  - `MAX_THEMES_PER_CATEGORY=2` 규칙 내(반도체 2개)이지만, **반도체 카테고리만 동일 카테고리 중복**
  - 보유 포지션 5개 중: 삼성전자(전력반도체), 에이디테크놀로지(AI반도체), 네패스아크(AI반도체), LG디스플레이(아이폰), DL이앤씨(건설) → **반도체 계열 3종목 보유** (정확히는 아이폰 LG디스플레이를 제외해도 반도체 2개 + AI반도체 직접 노출)
  - 4/30 일간 -1.37% 손실의 주 원인은 미실현 -181K이며, **반도체 섹터 동조 하락 영향 가능성**
  - 다만 카테고리 자체는 실제 다양함(반도체 2 + IT서비스 1 + 산업재 1 + 기타 1) → MAX 2개 규칙 내 정상

- **제안 여부**: → **정보 공유만**. `MAX_THEMES_PER_CATEGORY` 규정 위반은 아니며, 추가 표본 필요.

---

## 5. 파라미터 조정 제안

### 제안 1 (시급, 코드 조사 — 파라미터 변경 아님): trade_reviews 적재 누락 원인 조사

| 항목 | 내용 |
|-----|------|
| 발견 | 4/27 한화오션 보유기간 매도가 trade_reviews에 미적재 (발견 1) |
| 영향 | AI 사후 분석 미실행, post_trade_prices 누락, hold_days_review 데이터 정체 |
| 권고 행동 | `run_hold_period_sells()` → `execute_sell_orders()` → `_close_position_in_db()` 호출 체인을 점검. 다른 매도 경로(트레일링/손절)는 정상이므로 보유기간 전용 경로의 분기 로직 확인 |
| 파라미터 변경 | **없음** (코드 버그 의심) |
| 신뢰도 | **High** (DB 직접 검증, 명백한 누락) |

→ 다음 사이클 시작 전 **strategy-coder /plan**으로 별도 처리 필요. 제안서 발의가 아닌 **버그 트리아지** 사항.

### 제안 2: Phase A 관찰 1주 연장 + Phase B(MIN_FINAL_SCORE 45→42) 보류

| 파라미터 | 현재값 | 제안값 | 근거(발견) | 예상 임팩트 | 신뢰도 |
|---------|-------|-------|----------|----------|--------|
| RSI_DYNAMIC_ENABLED | True | **유지** | 발견 2 | 매수 실패일 0/4, 통과율 +2.7%p — 효과 확인 | Medium |
| THEME_MIN_SLOT_ENABLED | True | **유지** | 발견 2 | 슬롯 보장 96% 발동, 5테마 진출 4/5일 | Medium |
| THEME_SAFETY_FLOOR | 25.0 | **유지** | 발견 2 | 보장 통과 종목 최저 30점, 25점 미만 통과 0건 — 현재 floor가 너무 보수적이지도 너무 관대하지도 않음 | Medium |
| MIN_FINAL_SCORE | 45 (Phase B 후보) | **변경 보류** | 발견 2, 4 | Phase A 매수 종목들이 아직 매도 안 됨. 매도 후 5일 평균 수익률 측정 후 결정 | Low |

**판단 근거**:
- Phase A 1주 관찰의 **롤백 트리거 모두 미발동** → Phase A 유지 결정
- 다만 **Phase A에서 진입한 종목들의 매도 성과(트레일링 발동 후 수익률)가 아직 미수집** — 이 데이터 없이 Phase B(매수 점수 컷 완화)로 넘어가면 위험 누적 가능성
- `project_buy_filter_phase_a.md` 명시: "Phase B는 Phase A 1주 관찰 결과 미흡 시" — 미흡 신호 없으므로 **Phase B 자동 유보**

### 제안 3 (정보성): 손절·트레일링·보유기간 파라미터 — 본 사이클 변경 없음

| 영역 | 현재값 | 변경 제안 | 사유 |
|------|-------|---------|------|
| STOP_LOSS_FAST | -0.07 | **유지** | 평시 손절 표본 2건만 존재(트리거 10건 미달) — 발견 4 |
| GRACE_PERIOD_DAYS | 1 | **유지** | 동상 — Phase B 연기 |
| TRAIL_LEVEL2_PCT | 0.03 | **유지(monthly에서 재검토)** | 발견 3은 4/22 직전 데이터, 본 주간 표본 0 — 단독 결정 불가 |
| MAX_HOLD_DAYS_PROFIT | 10 | **유지** | 발견 1로 인해 보유기간 매도건의 사후 평가 데이터가 누락된 상태 — 데이터 복구 후 재평가 |
| MAX_GAP_UP/DOWN_PERCENT | 3.0 / 2.0 | **유지** | screening_log에 'gap' 사유 reject 0건 (집계 불가) |

**신뢰도 판정**:
- High: 시스템 이슈(발견 1) — 명확한 DB 증거
- Medium: Phase A 통과율 변화(발견 2) — 5영업일 표본
- Low: 매도 성과 표본(주간 1건)

---

## 6. 미결 검토 항목 결론

### project_stop_loss_review.md
- **결론**: **진행(연기 유지)** — 평시 손절 ≥10건 트리거 미충족 (현재 2건). HPSP/키움 2건 정반대 결론으로 결정 불가.
- **재평가 시점**: 추가 5~8건 평시 손절 매매 누적 후. 예상 시점 5월 후반~6월 초.

### project_gap_filter_review.md
- **결론**: **미결** — 본 주간 screening_log에 gap 관련 reject_reason 0건 (모두 'technical/liquidity/supply'로만 기록). reject_reason 상세 분류 부족으로 갭 필터 적중률 측정 불가.
- **권고**: 향후 `screening_log.reject_reason`에 갭 사유를 명시 분리 (예: `gap_up`, `gap_down`) 하는 코드 개선 별도 검토.

### project_hold_days_review.md
- **결론**: **미결(시스템 이슈로 정체)** — 본 주간 보유기간 매도 1건이 trade_reviews 미적재(발견 1). 누적 max_hold 매도 표본 = 2건 (4/17 한국조선해양, 4/27 한화오션 *누락*).
- **권고**: 발견 1 시스템 이슈 해결 → 한화오션 trade_review 소급 재생성 → AI 분석 트리거 → 데이터 누적 재개.

### project_buy_filter_phase_a.md (이번 주차 메인 검토 대상)
- **결론**: **Phase A 유지, 롤백 미발동** — 통과율 개선, 슬롯 보장 96% 발동, 매수 실패일 0/4. **Phase B 변경(MIN_FINAL_SCORE 45→42) 자동 유보**.
- **다음 사이클**: 2026-W19 또는 W20에 Phase A 매수 종목들의 매도 성과 5건 이상 누적 시 Phase B 재검토.

---

## 7. 기각된 가설

1. **"5/1 매수 0건은 시스템 이슈"** → 기각: 5/1은 근로자의날 휴장 가능성. KRX 휴장일이면 시스템이 정상 동작. (`is_trading_day()` 함수의 `holidays.KR` 라이브러리는 근로자의날 미포함 가능성 있어 후속 점검 필요. 발견 1과 별개 이슈로 분리.)
2. **"트레일링 L1/L2 너무 빠름, 즉시 변경 필요"** → 기각: 본 주간 트레일링 발동 매도 0건. 발견 3은 직전 주차 데이터 재참조이며, 단독 변경 근거 부족.
3. **"손절 -7% → -5% 즉시 적용"** → 기각: 평시 표본 2건, 트리거(10건) 미달. project_stop_loss_review.md에 명시된 연기 결정 유지.
4. **"5테마 중 반도체 2개는 위험 집중"** → 기각: MAX_THEMES_PER_CATEGORY=2 규칙 내, 카테고리 다양성 5/5 (4/5? — 반도체 1+1, IT 1, 산업재 1, 기타 1 = 4 카테고리). 추가 표본 필요.

## 8. 다음 사이클 관찰 항목

- [ ] **(시급)** 발견 1: trade_reviews 적재 누락 원인 조사 + 한화오션 trade_review 소급 생성 → strategy-coder `/plan`으로 별도 진행
- [ ] Phase A 매수 종목들(LG디스플레이, 네패스아크, 에이디테크놀로지, DL이앤씨, 삼성전자) 매도 후 D+5 수익률 추적 — 5건 이상 누적 시 Phase B 재검토
- [ ] 평시 손절 매매 추가 누적 (현재 2건 → 목표 ≥10건)
- [ ] reject_reason 상세 분류 개선 (현재 카테고리 단위만 기록 → 'gap_up'/'rsi_high' 등 분리)
- [ ] 5/1 근로자의날 처리 점검 — `holidays.KR` 라이브러리에 한국 근로자의날 포함 여부 확인 (별도 점검)
- [ ] 5/1 전기차 테마 RSI 평균 77.8(max 100) → 슬롯 보장 발동 실패 → 이런 상황의 fallback 정책 검토 (예: 본 테마는 그날만 슬롯 면제)
- [ ] 4/30 미실현 -181K 회귀의 종목별 기여 분해(반도체 동조성 검증)

## 9. 메타 정보

- **Claude API 추가 호출**: 아니오 — 정량 지표만으로 판정.
- **WEEKLY_SUMMARY_PROMPT 결과 흡수**: **아니오** — 본 주간 trade_reviews 적재 0건 → `generate_weekly_summary()` 입력 데이터 없음. 자동 흡수 불가.
- **JSON 파싱 실패 건수**: **0** (본 주간 ai_review 자체가 0건이므로 파싱 대상 없음).
- **NULL 대기 vs 분석 지연**:
  ```sql
  SELECT id, stock_code, sell_date,
         CAST(julianday('now','localtime') - julianday(sell_date) AS INTEGER) AS days_since_sell,
         CASE WHEN ... < 8 THEN '대기중' ELSE '분석지연' END AS status
  FROM trade_reviews WHERE ai_review IS NULL AND sell_date >= '2026-04-01';
  ```
  결과: **0건** — trade_reviews에 4월 NULL 행 없음. (단, 발견 1의 한화오션 4/27 매도건은 **`trade_reviews` 자체가 누락**이라 본 쿼리에 잡히지 않음. 시스템 이슈 별도.)
- **에이전트 자기 점검 완료**: 예
  - [x] 모든 파라미터 제안에 쿼리/수치/신뢰도 있음 (또는 명시적 변경 없음)
  - [x] parameter_suggestion 자유 서술 필드 건수 집계 안 함 (인용만 사용)
  - [x] 민감 데이터 미포함 (계좌잔고/주문ID 노출 없음)
  - [x] Claude API 추가 호출 없음
  - [x] change_log.md 최근 변경(2026-04-24 Phase A) 섹션 2에 반영
  - [x] 미결 검토 항목 4건(stop_loss/gap_filter/hold_days/buy_filter_phase_a) 모두 섹션 6에 반영
  - [x] 파일명 규칙 준수: `2026-W18-weekly.md` (ISO 18주차)
  - [x] 메인 에이전트 리턴용 3줄 요약 준비

## 구현 시 안내 (사용자 → strategy-coder 시)

본 제안서는 **파라미터 변경 0건**이지만, **시스템 이슈(발견 1)** 처리 시 다음 절차를 따른다:

1. 이 제안서를 근거로 `/plan docs/improvements/2026-W18-weekly.md` 또는 별도 버그 트리아지 플랜 발의
2. trade_reviews 적재 누락 코드 수정 후 한화오션 4/27 매도건 소급 trade_review 생성
3. `docs/improvements/change_log.md`에 1줄 추가 — **단, 파라미터 변경이 아니므로 표 행 추가 대신 비고란 또는 별도 섹션에 "버그 픽스 — trade_reviews 적재 누락 원인 수정"** 형식으로 기록 권고
4. Phase A 유지 결정도 change_log.md에 별도 행 추가 가능 ("관찰 결과 유지 결정")

---

**결론**: 본 주간은 **Phase A 1주 관찰 사이클의 종료**로서 효과 검증은 긍정적이지만(통과율 개선, 슬롯 보장 효과적), **trade_reviews 적재 누락이라는 시스템 이슈 발견**이 더 시급하다. 파라미터 변경 제안은 본 사이클에서 유보하며, 다음 사이클에 Phase A 매수 종목 매도 표본 누적 후 Phase B 재평가를 권고한다.
