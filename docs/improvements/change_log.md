# 파라미터 변경 이력 (before/after 추적용)

`trade-improvement-analyst` 에이전트의 제안 → 사용자 승인 → strategy-coder 구현으로 이어진 **파라미터 조정의 단일 진실 공급원(single source of truth)**이다. 다음 분석 사이클에서 에이전트가 이 파일을 읽어 변경 전/후 성과를 비교 보고한다.

## 기입 규칙

- **누가**: strategy-coder (CHECKLIST 배포 항목에서)
- **언제**: 제안 구현 후, 배포(재시작) 직전 또는 직후
- **어떻게**: 아래 표에 **1줄 추가만**. 기존 행 편집 금지. 롤백/재조정은 새 행으로 기록.
- **필수 항목**: 날짜, 파라미터명, 이전값, 변경값, 제안서 경로, 승인자

## 변경 이력

> 에이전트 도입(2026-04-21) 이전 변경 3건은 **소급 기록**(제안서 경로 대신 커밋 해시)이므로 "제안서 경로" 칼럼에 `commit:<hash>` 표기. 이후 변경은 반드시 에이전트 제안서 경로를 기록할 것.

| 날짜 (KST) | 파라미터명 | 이전값 | 변경값 | 제안서 경로 | 승인자 | 비고 |
|-----------|-----------|-------|-------|------------|--------|------|
| 2026-04-14 | TRAIL_BE_ACTIVATION / TRAIL_BE_STOP | (미구현) | +5% 도달 시 매수가 -1% 손절 | commit:562e1d5 | hatni | 소급 기록. +5% 도달 후 하락 케이스(오이솔루션형) 방어. Phase 1 focus:stop_loss 제안서가 효과 측정 대기 중 |
| 2026-04-16 | ORDER_TYPE_DEFAULT | "market" (시장가 01) | "limit_aggressive" (지정가 00 + 매도 1호가) | commit:7161210 | hatni | 소급 기록. 증거금 1.3→1.04배로 슬롯당 실제 투자금 73→91% 확대 목표. Phase 5 실전 관찰 중 |
| 2026-04-21 | THEME_MOMENTUM_BOOST_FACTOR / CLAMP / DROP_COOLDOWN | (기존 ×1.5 무제한, 쿨다운 없음) | factor 0.7 / clamp ±8 / top_k 30 / cooldown ON | commit:d820638 | hatni | 소급 기록. 화요일 테마 재선정 회전문 방지 Phase 1+2 |
| 2026-04-24 | RSI_DYNAMIC (RSI_UPPER_BULL/NORMAL/BEAR) + THEME_MIN_SLOT | RSI 70 고정 / 테마 슬롯 보장 없음 | 강세장 75 / 평시 70 / 약세장 65, 테마당 최소 1개(≥25점) AI 검증 보장 | docs/improvements/2026-04-23_buy_filter_proposal.md | hatni | Phase A 제안 #2+#3 동시 배포. DB v14 마이그레이션 (rsi_at_screen, theme_slot_protected 컬럼). 1주 관찰 후 롤백 트리거 체크 |
| 2026-05-01 | trade_reviews 적재 경로 (시스템 무결성 fix) | main.py 매도 3경로(보유기간/midweek 수익/midweek 손실)에서 save_trade_review 미호출 → 4월 SELL 18건 vs reviews 14건 (5건 누락) | main.py에 `_save_trade_review_for_main_sell` + `_compute_hold_days` 헬퍼 추가, 3경로에서 trade_id 캡처 + 헬퍼 호출. 누락 5건은 `scripts/recover_missing_trade_reviews.py`로 소급 복구 | docs/improvements/2026-05_monthly.md | hatni | 파라미터 변경이 아닌 데이터 무결성 버그 fix. monthly 분석에서 발견. 19건 review_count=1 검증 완료. systemd 재시작 (PID 1008604→1947127). 알려진 한계: midweek strategy_type="manual"로 분류, 소급 3건 max_profit_during_hold NULL |
| 2026-05-01 | RSI_DYNAMIC + THEME_MIN_SLOT (Phase A 유지 결정) | (4/24 배포 동일) | (변경 없음, 1주 관찰 후 유지) | docs/improvements/2026-W18-weekly.md | hatni | Phase A 1주 관찰 결과: 통과율 17.9%→20.6%, 매수 실패일 0/4, 슬롯 보장 19건 발동, 롤백 트리거 9.1절 5개 모두 미발동. Phase B(MIN_FINAL_SCORE 45→42)는 매도 표본 N=1로 보류, 5/15 재평가. 작업 디렉토리 active→completed 아카이브 |
| 2026-05-01 | screening_log 다단계 로그 (W19 분석 데이터 기반) | screening_log.stage 단일값 'filter'만 존재 (2,985건) — 갭 필터/AI 검증 단계 흔적 부재 | morning_screener.py에 `_save_gap_filter_logs` (stage='gap_filter'), verifier.py에 `_save_ai_verify_logs` (stage='ai_verify') 추가. 통과/탈락 + reject_reason + score(AI sentiment) + details_json 기록 | docs/improvements/2026-05_monthly.md | hatni | 파라미터 변경이 아닌 데이터 인프라 fix. UNIQUE(date, stock_code, stage) 이미 존재해 마이그레이션 불필요. 코드 추가만으로 구현. 매수 흐름과 try/except 격리. systemd 재시작 (PID 1947127→1965133). W19 (5/8) `focus:gap_filter` 트리거 전 데이터 누적 시작 |
| 2026-05-04 | 매도 슬리피지 측정 (sell-slippage-tracking, 데이터 인프라 fix) | trades.slippage 95% NULL (55건 중 52건) — 시장가 매도 order_price=0이라 분모 0 + 5경로 모두 _save_trades 우회 | trading_engine에 `_capture_sell_reference_price`(bid1→current_price→fallback) + `_compute_sell_slippage` 헬퍼 추가, execute_sell_orders/stop_loss/take_profit 3진입점에서 result에 reference_price/source/slippage 채움. monitor 4함수(_close_position/_save_partial/_execute_max_hold/_execute_trailing) + main.py 3경로(보유기간/midweek 수익/손실)에서 result.get("slippage") 추출 후 db.save_trade에 전달. _save_trades 매도 분기는 order["slippage"] 우선, reference_price 폴백 | docs/work-plans/active/sell-slippage-tracking/PLAN.md | hatni | 파라미터 변경이 아닌 데이터 인프라 fix. DB 스키마 변경 없음(v9 slippage 컬럼 활용). 단위 테스트 11건 PASS, code-tester 심각 0건. 공격적 지정가 매수(2026-04-16) 효과 평가의 매도 측 카운터파트 지표 확보 목적. 장 마감 후 systemd 재시작 예정 → 5/5(화) 매도부터 측정 시작 |
| 2026-05-04 | 종가베팅 시스템 Phase 1 (closing-bet-system, 신규 모듈 도입) | (신규 시스템 — 기존 파라미터 변경 아님) | closing_bet_system/ 9 모듈 신규 (cost_engine/2 collector/score_engine/2 risk/logger/notifier/orchestrator) + main.py 통합 (register_jobs로 APScheduler 잡 3건 추가, placeholder providers로 무동작 상태 유지) + 신규 텔레그램 봇 (chat_id=8509696011 스윙과 단일 채널) | docs/work-plans/active/closing-bet-system/PLAN.md | hatni | Phase 1 = 알림형 (자동매수 절대 금지). 9/9 단위 완료, 200+ 테스트 PASS, 모든 모듈 code-tester 통과. main.py 통합은 placeholder universe (빈 리스트) 라 잡 등록되지만 무동작 → 안전. Phase 2 진입 시 universe_provider/market_data_provider/name_lookup/label_provider 실 구현체 연결 필요. 1-9 운영 점검 게이트 (30건 누적) 데이터 수집 시작점 |
| 2026-05-04 | 종가베팅 universe v2 (closing-bet-universe-v2, 데이터 인프라 확장) | universe v1 = 스윙 top_themes 5개에서만 추출 (PRD 16-3 Layer 3 부분만) → 19종목/일. PRD 핵심 시그니처(거래대금 폭발/외국인 매수 우위/모멘텀 강세) 종목 누락 — 30건 게이트가 PRD 시그니처로 채워지지 않는 한계 | `closing_bet_system/collectors/universe_provider_v2.py` 신규 (4 출처 합집합: 스윙 테마 + pykrx 거래대금/등락률/외국인 순매수 상위 30) + `universe_filters.py` 신규 (PRD 4-1 시총 500억/주가 1000원/상한가 +29.5%/52주 고점 -30% + 4-4 20일 평균 50억/당일 거래대금 100억) + `get_universe_v2_filtered()` 통합. pykrx 활용 (KIS API 부담 0). 외국인 컬럼은 `순매수거래대금` 정확 매핑 (code-tester 심각 1건 차단). hard_cap 100, 보수적 하드 필터 (데이터 없으면 탈락) | docs/work-plans/active/closing-bet-universe-v2/PLAN.md | hatni | 데이터 인프라 확장. DB 스키마 변경 없음. 단위 테스트 33건 PASS (2-9a 18건 + 2-9b 15건), code-tester 심각 0건. scheduler.py 교체 + 단발 검증 + 배포는 별도 단계. 자동매매 위험 0 (Layer 1 가중치 0 → 알림 임계값 7점 미달이라 알림 0건 유지). 30건 운영 점검 게이트 데이터 품질 확보 |
| 2026-05-04 | 종가베팅 Phase 2 데이터 수집 (closing-bet-phase2-data-collection, 옵션 A 3단위) | Phase 1 이월 후 자동매매 30건 게이트 누적 대기 기간에 수집/모니터링 인프라 추가 (호가 데이터 0건 / KIND 통합 인터페이스만 / 대시보드 종가베팅 탭 부재) | 단위 2-1: kis_orderbook_collector(KIS inquire_asking_price 호출 → ask1/bid1/잔량/스프레드 OrderbookSnapshot, DB v2 orderbook_snapshots 테이블 + 2 인덱스, main_orchestrator asyncio.gather 4 collector 병렬 + DB INSERT). 단위 2-2: kind_alert_collector(KindAlertSnapshot dataclass + provider 주입 + 4단계 한글명→severity 매핑, OvernightRiskFilter assess/assess_for_universe에 kind_alerts 인자 + _resolve_kind_severity 헬퍼, severity 3→can_enter=False / 2→size 0.5 / 1→warnings) — 1단계 인터페이스만 활성, KindHttpProvider는 후속 단위. 단위 2-6: dashboard data_adapter(5 read-only 헬퍼) + web/api_routes 5 엔드포인트(/api/v1/closing-bet/{today,gate-progress,orderbook-history,rejections,fund-guard-status}) + dashboard.html "종가베팅" 탭(loadClosingBet, 5 카드/테이블 섹션). | docs/work-plans/active/closing-bet-phase2-data-collection/PLAN.md | hatni | 자동매매 위험 0 (모두 데이터 수집/표시). 단위 테스트 42건 PASS (OB 13 + KA 16 + DA 13). code-tester 심각 2건(insert_snapshots db=None 누수, assess_for_universe kind_alerts 누락) + 주의 2건 모두 보강. 종가베팅 첫 잡 트리거(15:10 KST) 이후 KIS API 추가 ~80호출/일 예상. KIND 실 데이터는 사이트 구조 안정성 검토 후 별도 단위로 도입 예정. systemd + dashboard 동시 재시작 (PID 2511049→2533276 / 2295896→2533412). |
| 2026-05-04 | 종가베팅 KIND 시장경보 네이버 프로바이더 (closing-bet-kind-naver-provider, PRD 4-1 정합도 향상) | KindAlertCollector(provider=None) → 빈 dict 항상 반환 → PRD 4-1 "관리/투자경고/거래정지 제외" 하드 룰 코드만 있고 실데이터 부재 (정합도 ⚠️ 부분) | `closing_bet_system/collectors/kind_naver_provider.py` 신규 (네이버 금융 5 페이지 fetch: 관리종목/거래정지/투자주의/경고/위험, urllib + EUC-KR + LEVEL_PRIORITY 우선순위, try/except 격리, 1초 내 315종목 수집) + `kind_alert_collector.py`에 "관리종목": 3 매핑 추가 (PRD 4-1 정합) + `universe_filters.py` apply_attribute_filters/apply_all_filters에 `severity_map: Optional[dict]` 인자 추가 + severity ≥ 3 사전 제외 로직 (속성 필터 첫 단계, first-rejection-only) + `universe_provider_v2.py` get_universe_v2_filtered에 severity_map 전달 + `main_orchestrator.py` kind_collector property에 KindNaverProvider 주입 + run_daily_pipeline 호출 순서 변경 (KIND fetch → universe 산출 → 4 collectors 병렬) + lambda wrap으로 v1 시그니처 호환 폴백 (TypeError 시 무인자 호출 + warning) | docs/work-plans/active/closing-bet-kind-naver-provider/PLAN.md | hatni | PRD 정합도 향상 (4-1 KIND 항목 ⚠️→✅). DB 스키마 변경 없음. 단위 테스트 25건 PASS (2-2b-1 15 + 2-2b-2 10), 회귀 누적 99건 PASS. code-tester 심각 0건/주의 3건 모두 즉시 반영 (로그 카운트 분리/TypeError warning/SEVERITY_EXCLUDE_THRESHOLD 단방향 import 통합). 자동매매 위험 0 (Phase 1 알림형 유지). 네이버 1회/일 호출 차단 위험 낮음. systemd 재시작 (PID 2695913 → 2732694, KST 21:18) — 5/6(수) 15:10 첫 자연 트리거에서 KIND severity ≥ 3 종목 5~10건 사전 제외 예상 |
| 2026-05-04 | 종가베팅 Phase 1 이월 항목 (closing-bet-phase1-carryover, providers 활성화) | placeholder providers 4종 모두 무동작 (universe=[], market_data={}, name_lookup="(미상)", label_provider=None) + fund_guard.weekly_loss_limit 미구현 + telegram_client 부모 내부 속성 직접 변경 (silent break 위험) | 단위 A: name_lookup.py(신규, KIS get_stock_name+캐시) + telegram_client.py(NoOp 더미 패턴). 단위 B: universe_provider.py(신규, 스윙 top_themes→네이버 크롤링→list[ticker], 캐시+검증+중복제거+스윙보유제외+hard_cap=20). 단위 C: market_data_provider.py(신규, KOSPI=KIS+V-KOSPI/미선물/USD-KRW=yfinance, Phase 1 결손정책 None 폴백). 단위 D: label_provider.py(신규, KIS daily 첫 행 + 라벨 4종 + cost_engine 통합) + main_orchestrator.py(label_provider 인자 추가, run_label_yesterday 인스턴스 폴백). 단위 E: fund_guard.py(weekly_loss_limit 8번째 검사 추가, _fetch_db_state 단일 connection 4쿼리). scheduler.py 4 lambda → 4 실 함수. | docs/work-plans/active/closing-bet-phase1-carryover/PLAN.md | hatni | 5단위 단위 테스트 55건 PASS (NL/TG 12 + UV 10 + MD 11 + LP 11 + WL 11). code-tester 검증 완료 (단위 A 심각 0/주의 2 / 단위 B-E 종합 심각 0/주의 4 — false alarm 1건은 KIS 정렬 방향 명시 주석+sanity check로 보강, 나머지 P2 4건은 주석/로그 개선으로 보강). DB 변경 없음. 종가베팅 Phase 1 잡 3건 (15:10/15:35/10:00) 실제 동작 가능 — universe 비-empty 시 알림 발송. Phase 2 (반자동) 진입 준비 완료. |
| 2026-05-06 | 화요일 공휴일 보정 재선정 (theme-makeup-reselection) | 화요일이 공휴일이면 `_skip_on_holiday` 데코레이터로 정규 테마 재선정(08:30 KST `_run_theme_analysis`)이 스킵 → 다음 정규 재선정은 14일 후로 미뤄짐 (2026-05-05 어린이날에 발생) | config.py에 `is_makeup_reselection_day(today, target_weekday=1)` 순수 함수 + `THEME_MAKEUP_RESELECTION_ENABLED=True` 토글 추가. main.py 4곳(import / `run_theme_analysis` 시작부 today/is_makeup 미리 계산 + 중복 발화 방어 / 정규 재선정 진입 직전 보정 알림 logger+텔레그램 한글 요일 / `check_theme_rotation` is_review_day 식 / `_check_midweek_replacement` 화요일 가드). 보정 발화 조건: today 영업일 AND 직전 화요일 비영업일 AND 사이에 영업일 없음 AND `_last_theme_rotation_date < missed_tue` (중복 방어) | docs/work-plans/active/theme-makeup-reselection/PLAN.md | hatni | 사용자 관찰 기반 운영 개선 (제안서 없이 진행 — 파라미터 변경 아님, 운영 정합성 fix). Plan 에이전트 + 코더 에이전트 병렬 리뷰 후 진행. 단위 테스트 8건 PASS (시나리오 5개 + 엣지 3개), code-tester GO (심각 0 / 주의 2건 모두 즉시 패치: 콜드 스타트 알림 생략 + 한글 요일 + None 방어). 별도 작업 후보: same_week 분기의 매일 selected=True 동기화 의미 정정 / KRX 임시휴장일+근로자의 날 캘린더 보강 / APScheduler misfire_grace_time 정책. 롤백: `THEME_MAKEUP_RESELECTION_ENABLED=False` + systemctl restart |
| 2026-05-06 | 종가베팅 universe v2 KRX bulk 우회 (closing-bet-universe-v2-bulk-bypass, 단위 2-9c) | KRX 사이트 정책 변경으로 pykrx `get_market_*_by_ticker`(시장 단위 bulk) 빈 응답 → 5/6 15:10 KST universe v2 = 0건, 파이프라인 스킵. pykrx 1.2.3 KeyError, 1.2.8 KRX_ID/KRX_PW 인증 요구로 운영 부담 큼. 종목별 `get_market_ohlcv_by_date`는 정상 작동 | `universe_filters.py` `_fetch_per_ticker_today_data` 헬퍼 신규 + `_fetch_market_data_bulk(today_str, tickers=None)` 시그니처 확장 + `_maybe_run_per_ticker_fallback`/`_merge_with_fallback_cache` 폴백 분기. 옵션 A 보수: 시총 None → `data_not_found` 탈락(폴백에서 시총 채우지 않음, 시총 보강은 단위 2-9d). settings.yaml `data_source.fallback_per_ticker_enabled: true` 토글 신설(default true). `apply_attribute/liquidity_filters` 호출부 tickers 전달. 모듈 상수 `MAX_FALLBACK_TICKERS=100`(hard_cap 정합)/`FALLBACK_RATE_LIMIT_SEC=0.05`. 별도 `_per_ticker_market_cache`로 bulk 캐시 격리 | docs/work-plans/active/closing-bet-universe-v2-bulk-bypass/PLAN.md | hatni | 즉시 운영 복귀 인프라 (5/7 15:10 자연 트리거에서 폴백 진입 로그 검증). 단위 테스트 10건 PASS(UF-C-1~10), 회귀 58건 PASS(2-9a 18 + 2-9b 15 + 2-2b-1 15 + 2-2b-2 10). code-tester 심각 2건 발견 즉시 수정(시총 None 보수 탈락 정합 / 토글 로드 예외 시 default 명시 체크), 데드락 1건(`_merge_with_fallback_cache`의 lock 재획득) 발견 즉시 수정. 통합 단발: 폴백 17/17 성공 2.34초, 옵션 A 정합 universe = 0건. 자동매매 위험 0(Phase 1 알림형). 본격 해결은 단위 2-9d (KIS Open API ranking 대체) — 새 대화 진행. 롤백: settings.yaml `fallback_per_ticker_enabled: false` + systemctl restart |
| 2026-05-06 | 종가베팅 universe v2 KIS Open API ranking 대체 (closing-bet-kis-bulk-replacement, 단위 2-9d) | 단위 2-9c 폴백 인프라 + 옵션 A 보수 탈락(시총 None → data_not_found)으로 universe = 0건. KRX bulk 차단의 본질 해결을 위해 KIS Open API ranking 4종으로 출처 2~4 대체 + KIS API 활용. settings.yaml `kis.use_mock=true` 와 메인 봇 `--real` 충돌로 토큰 인증 403 → 실전 모드로 통합 | `closing_bet_system/collectors/kis_market_provider.py` 신규(~280줄, KISMarketProvider 클래스 + 싱글톤, 4 ranking 메서드: get_top_value_codes(FHPST01710000, BLNG_CLS=3) / get_top_change_codes(FHPST01700000, direction=up/down) / get_top_foreign_buy_codes(FHPTJ04400000, ETC_CLS=1) / get_top_market_cap_data(FHPST01740000), 모듈 상수 TR_ID 4종+URL 4종+응답 컬럼명, _call_ranking 공통 헬퍼, KISApi 인스턴스 위임으로 _shared_token 재사용, _safe_int/_safe_float NaN 가드, 6자리 정규식 검증, rt_cd!=0 graceful) + `universe_provider_v2.py` `_is_kis_ranking_enabled()` 토글 헬퍼 + 출처 2~4 본문 KIS 라우팅 분기(use_kis_ranking=true → KIS / false 또는 실패 → pykrx 폴백) + `universe_filters.py` `_load_filter_config` 에 fallback_include_market_cap/kis_market_cap_top_n 키 추가 + `_maybe_run_per_ticker_fallback` 안 KIS market-cap 시총 보강 분기(현재 OFF) + cfg 명시적 default 폴백(NameError 방어) + settings.yaml `kis.use_mock: false` 전환(메인 봇 실전 통합) + data_source.use_kis_ranking/fallback_include_market_cap/kis_market_cap_top_n 토글 신설 | docs/work-plans/active/closing-bet-kis-bulk-replacement/PLAN.md | hatni | KIS 매뉴얼 검증: koreainvestment/open-trading-api 공식 GitHub WebFetch 4개 파일 직접 fetch + apiportal 포탈 확인. 단위 테스트 12건 PASS(KP-1~12), 회귀 누적 80건 PASS(2-9a 18 + 2-9b 15 + 2-9c 10 + 2-9d 12 + 2-2b-1 15 + 2-2b-2 10). code-tester 심각 3건 + 주의 6건 발견 즉시 수정(심각#1 cfg NameError 명시 default / 심각#2 direction dead parameter / 심각#3 prdy_ctrt 하드코딩→상수 / 주의#3 stck_avls 단위 진단 로그 추가). 통합 단발: KIS ranking 정상 작동(theme=17 / top_value=25 / top_foreign=21 = 63종목 산출, 17→63 3.7배 증가) + KIS 토큰 발급 정상. ⚠️ stck_avls 시총 단위 미확정(005930=15,551,101 어떤 단위로도 삼성전자 시총 불일치) → fallback_include_market_cap=false default 처리, 시총 보강은 후속 단위 2-9e로 분리(종목별 inquire-price 보강 또는 단위 정규화 검증). 자동매매 위험 0(Phase 1 알림형). 5/7 15:10 자연 트리거 검증. 롤백: settings.yaml use_kis_ranking=false + use_mock=true + systemctl restart |
| 2026-05-06 | 종가베팅 universe v2 시총 보강 정규화 (closing-bet-market-cap-normalization, 단위 2-9e) | 단위 2-9d 후 KIS ranking/market-cap 응답 stck_avls 단위 미확정으로 fallback_include_market_cap=false → universe = 0건 한계. 5종목 단위 검증으로 stck_avls = 억원 확정 (005930 stck_avls=15,551,101 × 1억 = 1,555,110,100,000,000원 ↔ stck_prpr × lstn_stcn ↔ inquire-price market_cap 정확 일치) | `kis_market_provider.py` 라인 246: `entry["market_cap"] = mcap` → `int(round(mcap * _STCK_AVLS_UNIT_TO_WON))` 정규화 (모듈 상수 _STCK_AVLS_UNIT_TO_WON=100_000_000 신규, IEEE 754 정밀도 보호 위해 round 적용) + 인라인 주석/진단 로그 갱신 (5,000억 오기 → 500억 정정 + 단위 2-9e 확정 표기). `universe_filters.py` 라인 86 `DEFAULT_FALLBACK_INCLUDE_MARKET_CAP=True → False` (settings.yaml 의도 정합 + cfg fallback 안전망 역동작 봉쇄) + `_maybe_run_per_ticker_fallback` docstring "bulk_result 빈 상태에서만 호출" 명시 추가. `settings.yaml` `data_source.fallback_include_market_cap=false → true` 활성. `scripts/test_closing_bet_unit_2_9e.py` 신규 (MC-1~8 + 보강 1b/6a/6b/7b 11건). `scripts/probe_kis_unit_2_9e.py` 보존 (재검증용) | docs/work-plans/active/closing-bet-market-cap-normalization/PLAN.md | hatni | Plan + strategy-coder 에이전트 병렬 사전 리뷰 (심각 3 + 주의 6건 발견 즉시 정정). 단위 테스트 11건 PASS (MC-1 정규화 정확성/MC-1b int 타입/MC-2 mcap=0 가드/MC-3 정규식/MC-4 시총 보강 통합/MC-5 옵션 A 보수 탈락/MC-6a 정규화 PASS/MC-6b 정규화 누락 회귀 FAIL/MC-7 토글 OFF 회귀/MC-7b settings 로드 실패 안전망/MC-8 NaN 가드), 회귀 누적 91건 PASS (2-9a 18 + 2-9b 15 + 2-9c 10 + 2-9d 12 + 2-9e 11 + 2-2b-1 15 + 2-2b-2 10). code-tester 심각 0건 / 주의 2건 모두 즉시 보강 (pandas top-level import + docstring). 통합 단발: universe = **18건** (목표 5건 4배 초과 달성, 시총 보강 18/53건 매치). ⚠️ 우선주 005935 universe 진입 발견 — 단위 2-9f 트리거 신호 (사용자 결정으로 본 단위 외). 자동매매 위험 0(Phase 1 알림형). 5/7 15:10 자연 트리거 검증 + 우선주 0건 점검 + pykrx bulk 정상 복구 의심 점검. systemd 재시작 (PID 3440709 → 3553333, KST 16:07). 롤백: settings.yaml fallback_include_market_cap=false + systemctl restart |
| 2026-05-06 | 09:00 조기 모니터링 + 테마 슬롯 조건부 상향 (partial-profit-early-monitoring-and-theme-slot-relax) | (1) 모니터링 시작 09:26 → 09:00~09:25 분할익절(L1+8%/L2+15%/L3+25%) 시그널 누락 위험 (사용자 관찰: 삼성전자/네페스아크 09:00 직후 급등). (2) 테마 한도 2 고정 → 빈 슬롯 남고 후보가 동일 테마라 매수 탈락 → 5종목 못 채움 | (1) 09:00 monitoring_start_early 잡 신규(scheduler.py, PARTIAL_PROFIT_EARLY_MONITORING_ENABLED 플래그 분기), main.py start_monitoring 재시작 패턴(09:26 stop+start로 신규 매수분 포함, 옵션 D-1 — KIS WS 동적 구독 미지원 우회), modules/trading_engine/sell_lock.py 신규(threading.Lock 기반 SellLockRegistry 싱글톤, owner-tagged), monitor 분할익절/손절/트레일링 3진입점에 acquire 가드(분할익절은 trigger_hit 조건부 acquire), main.py 매도 3종(hold_period 09:15 / midweek_profit 09:00 / midweek_loss 09:10) acquire 패턴(실패 종목만 스킵, 전체 잡은 계속), stop_monitoring에서 sell_lock.clear_all() 일괄 해제. (2) modules/trading_engine/diversity_filter.py 신규(apply_diversity_filter 순수 함수, 1차 한도 2 + 조건부 2차 한도 3, 카운터 dict 복사로 in-place 부작용 회피), main.py Phase 5.5 헬퍼 호출 대체 + _send_buy_summary에 "한도 상향 적용 N건" 표시. config.py 신규 상수 3개(PARTIAL_PROFIT_EARLY_MONITORING_ENABLED True / THEME_SLOT_RELAXATION_ENABLED True / MAX_STOCKS_PER_THEME_RELAXED 3) | docs/work-plans/active/partial-profit-early-monitoring-and-theme-slot-relax/PLAN.md | hatni | 사용자 관찰 기반 운영 개선 (제안서 없이 진행 — 분할익절 임계값/트레일링/손절률 무변경, 분산 한도 정책 변경은 점수 산식 외부). 단위 테스트 16건 PASS (sell_lock 6 + diversity_filter 10), code-tester 종합 "배포 가능" (심각 0 / 주의 2건 — 1건 즉시 패치 완료, 1건 09:26 clear_all follow-up). 시나리오 D(반도체×5, 빈슬롯 5)는 한도 3에서 멈춤(분산 우선, 사용자 확정). 롤백: 두 플래그 OFF + systemctl restart (setup_schedules 1회 호출 한계). 다음 거래일(5/7) 09:00~09:35 로그 관찰 — [SellLock] race 차단 메시지/모니터 재시작 로그/한도 상향 표시 빈도 |
| 2026-05-07 | 일일 헬스체크 13개 항목 확장 (health-check-upgrade) | before: 16:10 KST `run_daily_health_check`가 7개 항목만 체크 (PID/보유/매매/스냅샷/screening_log count/KIS/스케줄러). 테마 박제·Phase A·midweek 교체·slippage·SellLock 등 최근 작업 모니터링 부재. 공휴일 false alarm("테마 수집 미확인") 매년 3회 발생 | after: main.py 헬퍼 11개 신규(_hc_theme_score_pinning/screening_log_stages/phase_a_metrics/midweek_replacement/makeup_reselection/trade_review_coverage/slippage_coverage/systemd_dashboard/disk_usage/closing_bet_universe/sell_lock_residual) + 본체 재작성. 박제 분류 정밀화(zero=issue / NULL=info 폴백 대기 / mixed=정상). 공휴일 false alarm fix(`is_trading_day(yesterday)` 체크). subprocess/shutil/sqlite3 import 추가. 텔레그램 메시지는 신규 13개 정보 포함 ~534자(4096 한계 여유) | docs/work-plans/active/health-check-upgrade/PLAN.md (이번 세션) | hatni | 사용자 관찰 기반 운영 개선 (제안서 없이 진행 — 모니터링 인프라 확장, 파라미터/전략 무관). 단위 테스트 21건 PASS (TC 박제 4종 + screening_log 2종 + Phase A + midweek 2종 + makeup 3종 + review 2종 + slippage 2종 + disk + sell_lock 2종 + dashboard + closing_bet). code-tester 심각 0건 / 주의 2건 모두 즉시 패치 (공휴일 false alarm 1줄 + normal_count 불일치). NULL 분리 미리보기 재검증 후 박제 false alarm 해소. systemctl restart (PID 3865927→3889167→3891221). 5/8 16:10 KST 첫 자연 발화 검증 예정 |
| 2026-05-07 | 테마 점수 박제 회귀 차단 (theme-score-zero-fix, 시스템 무결성 fix) | before: themes 테이블 selected=1 5건이 5/4부터 매일 momentum=0/ai_sentiment=0/news_count=0 박제 (5/4~5/7 총 15 row). 원인 이중 버그: ① main.py:187-197 DB 복원 정규화에서 점수 4개 키 의도적 누락 + ② main.py:438 "기존 테마 유지" 분기 키명 불일치 (`t.get("momentum", 0)`인데 정규 재선정은 `momentum_score`) + ③ main.py:1950-1957 midweek 교체 dict 점수 키 누락 + ③' main.py:1997+ midweek 교체 후 DB 저장 동일 키명 불일치 (조사 중 추가 발견) | after: main.py 헬퍼 2개 신규(`_coalesce_zero` SQL NULL→0/실제0 보존 / `_pick_momentum` momentum_score→momentum 폴백 체인 is None 체크), DB 복원 시 박제 row 자동 감지 + `database.py:get_score_fallback_for_theme(theme_name, target_date)` 신규 메서드로 직전 selected=1 정상값 폴백, 4곳 수정(187-197 변경 1 / 438 변경 2 / 1950-1957 변경 3a / 1997+ 변경 3b), scorer.py:652 가드 코멘트 추가(momentum/momentum_score 양쪽 키 의존성 명시), 5/4~5/7 박제 15 row → NULL 마킹 (옵션 2, scripts/repair_theme_zeros_2026_05.py) | docs/work-plans/active/theme-score-zero-fix/PLAN.md | hatni | 사용자 관찰 + 두 에이전트 병렬 리뷰(strategy-coder + Plan) 후 진행. 시스템 무결성/저장 정합성 fix(파라미터 변경 아님). 단위 테스트 13건 PASS (TC-1·2·3·4·5·6 + Regression 시나리오), code-tester 심각 1건(WAL+서비스 중지 안내) + 주의 4건 즉시 패치(2건 반영/2건 후속), 단발 검증 스크립트 신규(scripts/verify_theme_restore_keys.py). 5/4~5/7 박제 15건 NULL 마킹 + systemd 재시작 시 5건 폴백 발동 확인 (전력반도체 17.57/플랫폼 15.51/전기차 13.89/건설 18.38/AI반도체 22.08). 5/8(금) 08:30 KST "기존 테마 유지" 트리거에서 정상값 저장 자연 검증 예정. 5/12(화) 정규 재선정 시 폴백 체인 호환 검증. systemctl restart (PID 3553333→3865927, KST 13:05). 후속(별도 PR): is_zero_pinned 조건 강화(셋 다 None 체크) — repair 스크립트 NULL 마킹 후 적용 가능 / midweek 통합 회귀 테스트 / `_pick_momentum` 헬퍼 4개 지점 통일. 롤백: git revert + DB 백업 복원(data/trading.db.backup_20260507_130501) |
| 2026-05-07 | 종가베팅 universe v2 ETF/우선주 차단 + lstn_stcn × stck_prpr 자체 시총 (closing-bet-unit-2-9f-etf-pref-block, 단위 2-9f) | before: 5/7 단위 2-9e 후 universe = 18건 / 우선주 005935 삼성전자우 진입 발견(단위 2-9e CHECKLIST 65줄 트리거 조건 충족) + 시총 보강 매치율 18/68 (KIS market-cap top 200 한도). KOSDAQ 보통주 false positive 위험으로 prefix/끝자리 룰 단순 적용 위험 | after: Plan + strategy-coder 사전 리뷰 (P0 블로커 2건) + Step 0 사전 조사 4단계로 결정사항 변경 — pykrx ETF 동적 조회 ❌(KeyError) → 종목명 brand prefix 매칭(KODEX/TIGER/KOSEF 등) 채택 / KIS `FID_DIV_CLS_CODE=1` volume_rank만 우선주 source-level 차단 검증 / KOSPI top 30 분포 실측: ETF 13/30(43%) + 우선주 진성 1건 + false positive **0건**(AND 조건 안전 검증) / 자기주식 영향 ≈ 0% 확정(top 5 시총 종목 +0.000% 차이). `universe_filters.py` 모듈 상수(_BLOCK_ETF_DEFAULT/_BLOCK_PREF_STOCK_DEFAULT/_PREF_STOCK_LAST_DIGITS/_PREF_STOCK_NAME_SUFFIXES/_ETF_BRAND_PREFIXES/_ETN_NAME_KEYWORD/REJECTION_REASON_*) + `_is_etf_or_etn(name)` / `_is_pref_stock(ticker, name)` 헬퍼 신규(AND 조건 + None safety) + apply_attribute_filters 시그니처에 `name_lookup_map` keyword-only 인자 + 차단 분기(KIND severity 다음, first-rejection-only) + 로그 분해 4단계(KIND/ETF/우선주/속성) + 첫 호출 진단 로그. apply_all_filters 시그니처에도 인자 추가. `kis_market_provider.py` `_FIELD_LISTED_SHARES` 상수 + `_compute_market_cap_from_response`(`_safe_int × _safe_int` IEEE 754 정밀도 손실 없음) + `get_top_value_data` 신규 메서드(volume_rank 응답 lstn_stcn × stck_prpr) + 단위 진단 로그. `_maybe_run_per_ticker_fallback` 시총 보강 2순위 분기 추가(1순위 단위 2-9d KIS market-cap top 200 미매치 → 2순위 단위 2-9f volume_rank top 30 자체 계산). `universe_provider_v2.py` get_universe_v2_filtered에 종목명 회수(name_lookup.get_name) + apply_all_filters에 name_lookup_map 전달 + TypeError 폴백. settings.yaml stock_filter.etf_block_enabled=true / pref_stock_block_enabled=false(점진 활성화) / data_source.kis_market_cap_priority=false / kis_div_cls_code="0" 토글 4종 신설 | docs/work-plans/active/closing-bet-unit-2-9f-etf-pref-block/PLAN.md | hatni | Plan + strategy-coder 에이전트 병렬 사전 리뷰(P0 블로커 2건 + 심각/주의 11건 발견 → PLAN/CONTEXT/CHECKLIST 갱신 후 진행). Step 0 사전 조사 4단계 모두 PASS(probe 3개 보존: probe_pykrx_etf_list/probe_kis_div_cls/probe_etf_pref_distribution). 단위 테스트 34건 PASS (5.1 ETF 5 + 5.2 우선주 5 + 5.3 자체 시총 4 + 5.4 통합 7 + 5.5 회귀 3 + 5.6 Edge 10), 회귀 누적 175건 PASS. code-tester 심각 0/주의 4건(모두 코드 무변경 주석 보완 — 2건 즉시 반영, 2건 참고). 통합 단발 검증: 종목명 회수 19/19 + 로그 분해 정상 + 시총 보강 1순위→2순위 우선순위 분기 동작. 자동매매 위험 0(Phase 1 알림형). 점진 활성화 정책: ETF 즉시(false positive pykrx 동적 조회 회피)/우선주 default false(1주 관찰 후 활성화 권장). systemctl restart (PID 3990656→4045208, KST 20:44). 5/8(금) 15:10 자연 트리거에서 우선주 진입 0건 + ETF 차단 N건 + universe 회귀 없음 검증 예정. 후속 단위: 2-9g KIS native filter 활성화 / 2-9h top_market_cap top_n 증가 / 2-9의 helper 분리 리팩토링 / Phase 2 자동매매. 롤백: settings.yaml 토글 4종 false 또는 default 복귀 + systemctl restart |
| 2026-05-07 | 종가베팅 universe v2 fluctuation ticker 필드 fallback (closing-bet-fluctuation-ticker-hotfix, 단위 2-9d 핫픽스) | before: 단위 2-9d 도입 후 KIS Open API ranking/fluctuation(FHPST01700000) 응답 종목코드 필드명이 `stck_shrn_iscd`(다른 3종 ranking은 `mksc_shrn_iscd`)인 회귀로 `_filter_valid_tickers`에서 30건 전부 정규식 탈락 → `[universe_v2] 출처별 기여 ... top_change=0` 영손실 일관 (5/6/5/7 자연 트리거 모두). PRD 16-3 Layer 3 등락률 모멘텀 시그니처 출처가 한번도 정상 작동한 적 없음 | after: `closing_bet_system/collectors/kis_market_provider.py` 모듈 상수에 `_FIELD_TICKER_ALT="stck_shrn_iscd"` + `_TICKER_KEYS_DEFAULT=(mksc, stck)` 신규. `_extract_ticker(item, ticker_keys)` 헬퍼 신규(None safe + 빈 문자열 탈락 + 첫 비-빈 값 사용). `_filter_valid_tickers` 시그니처에 `ticker_keys` keyword-only 인자 추가 + 본문에서 헬퍼 사용. `get_top_market_cap_data` 시총 보강 코드(라인 235)도 동일 패턴 재사용. `scripts/test_closing_bet_unit_2_9d_hotfix.py` 신규(HF-1~14 + _extract_ticker 헬퍼 직접 검증, assert 25건 PASS). `scripts/probe_kis_ranking_fields.py` 신규(4종 ranking 응답 필드 매핑 단발 조사, 회귀 재검증용 보존) | docs/work-plans/active/closing-bet-fluctuation-ticker-hotfix/PLAN.md | hatni | 5/7 19:15 KST 단발 조사로 회귀 원인 확정 (4종 ranking 응답 필드 매핑표 작성: volume_rank/foreign_total/market_cap=mksc, fluctuation=stck). 단위 테스트 25 assert PASS, 회귀 누적 116건 PASS (2-9a 18 + 2-9b 15 + 2-9c 10 + 2-9d 12 + 2-9e 11 + 2-9d-hotfix 25 + 2-2b-1 15 + 2-2b-2 10). code-tester 심각 0건 / 주의 1건(테스트 헤더 주석 14→25 정정) + 참고 2건(P3 non-string 가드, ticker_keys=빈 tuple 케이스). 통합 단발: 핫픽스 후 `get_top_change_codes(top_n=30)` → 30건 정상 반환 (002070 비비안 / 060900 에이전트AI / 276730 한울앤제주 — 5/7 raw 데이터 first 3 일치). mksc 우선 fallback으로 기존 동작 100% 보존 (volume_rank/foreign_total/market_cap 회귀 위험 0). 자동매매 위험 0 (Phase 1 알림형 유지). systemctl restart (PID 3891161→3990656, KST 19:39). 5/8(금) 15:10 자연 트리거에서 `top_change >= 1` 검증 예정. 추가 발견: market_cap/volume_rank 응답에 `lstn_stcn` 필드 존재 → 단위 2-9f(ETF/우선주 차단 + lstn_stcn × stck_prpr 자체 계산) 데이터 확보. 롤백: git revert + systemctl restart |
| 2026-05-08 | 종가베팅 Phase 2.5 백테스트 데이터 로더 (closing-bet-unit-2-7a-data-loader, 단위 2-7a) | before: candidates(56) + candidate_features(56) + candidate_labels(37) 3 테이블이 분산되어 있어 백테스트 시뮬/EV 분석 시 매번 ad-hoc SQL 작성 필요. Phase 2.5 진입 의사결정 근거(EV 측정/win rate/Sharpe) 도출 인프라 부재. 30건 운영 점검 게이트 통과(누적 49/30) 후 Phase 2.5 진입 단계 | after: `closing_bet_system/backtest/phase25_data_loader.py` 신규(257줄) — `load_phase25_dataset(start_date, end_date, *, db_path, statuses, only_labeled, only_features, return_meta)` 단일 진입점. LEFT JOIN 3 테이블 → pandas DataFrame(50컬럼) + meta dict 반환. dtype 후처리(trade_date Timestamp / 라벨 BooleanDtype pd.NA 보존 / pct float / is_labeled/is_featured bool). 빈 DataFrame도 dtype 명시(downstream 일관성). SQL injection 방어(parameterized binding + statuses placeholders 동적). 모듈 상수 `_DEFAULT_DB_PATH`/`_DEFAULT_STATUSES`/`_FEATURE_COLUMNS` 18 + `_LABEL_COLUMNS` 7 명시(스키마 정합 단일 변경점). CLI 진입점 `python -m closing_bet_system.backtest.phase25_data_loader --start ... --end ...` | docs/work-plans/active/closing-bet-unit-2-7a-data-loader/PLAN.md | hatni | Phase 2.5 백테스트 3 단위(2-7a/2-7b/2-7c) 중 데이터 접근 계층만 단독 단위로 격리(단일 책임 원칙). 자동매매 위험 0(읽기 전용 모듈, Phase 1 알림형 회로 무관). 단위 테스트 15/15 PASS (LD-1~15: 정상/status/only_labeled/only_features/빈 결과/FileNotFoundError/dtype/NULL pd.NA/정렬/return_meta/features 18/trade_date 카운트/날짜 호환/잘못된 날짜/statuses=() 전체). code-tester 심각 0건 / 주의 3건 모두 즉시 반영(UTC datetime → now_kst() / 빈 DF dtype 명시 / docstring 컬럼 수 35→50) / 참고 4건(WAL/모듈 import side-effect/CLI --only-features 누락/start>end 무검증)은 후속. 통합 검증: 5/4~5/8 실제 DB 로드 → shape (49, 50), labeled 33건, 셀트리온 백필 정상 포함(gap_up=True/stop_risk=True). systemd 영향 없음(읽기 전용 백테스트 모듈, 재시작 불필요). 다음 단위: 2-7b 시뮬레이터(PRD 12-1 라벨 4종 + 12-2 EV 계산식) → 2-7c 분석 리포트(walk-forward EV/win rate/Sharpe). 롤백: 신규 모듈 단독이라 git revert 즉시 가능(시스템 영향 없음) |
| 2026-05-08 | 종가베팅 label_provider KIS 일별시세 재시도 로직 (closing-bet-label-retry, 시스템 무결성 fix) | before: 5/8 10:00 KST 셀트리온(068270) `inquire-daily-price` 500 에러 1회 발생 → `_fetch_recent_ohlc` None 반환 → `run_label_yesterday` 자연 스킵(예외 카운트도 안됨) → 5/7 후보 18건 중 1건 영구 누락(candidate_labels 17/18). KIS 일시 장애가 데이터 손실로 직결되는 회로 | after: `closing_bet_system/collectors/label_provider.py` 모듈 상수 `_KIS_FETCH_MAX_ATTEMPTS=3` / `_KIS_FETCH_BACKOFF_SEC=5.0` 신설 + `_fetch_daily_price_with_retry(ticker)` 헬퍼 신규 (예외 시만 재시도, 빈 응답=정상 응답으로 간주하고 재시도 X — 비영업일/미상장 무한 대기 방지). `_fetch_recent_ohlc()` 가 새 헬퍼 호출. `closing_bet_system/main_orchestrator.py:438` `provider(ticker)` → `await asyncio.to_thread(provider, ticker)` 전환 (재시도 sleep이 이벤트루프 블로킹하지 않도록). 셀트리온 사례는 직접 INSERT로 백필 완료(candidate_id=31, gap_up=1/stop_risk=1/ev+=0) | docs/improvements/change_log.md (단발 fix, work-plan 미생성) | hatni | 사용자 관찰 기반 운영 개선 (제안서 없이 진행 — 무결성 fix, 파라미터/전략 무관). 단위 테스트 12건 PASS (RT-1~12: 첫 시도 성공/2차 복구/3차 복구/전체 실패/빈 응답 재시도 안 함/get_label 통합/잘못된 ticker/모듈 상수/backoff 호출 횟수/데이터 부족 회귀/warning 로깅). code-tester 심각 0건 / 주의 2건(이벤트루프 블로킹 → main_orchestrator 수정으로 해소 / 테스트 패치 스타일 P2 무시). KIS 토큰 1분 발급 제한 충돌 없음(get_kis_api 싱글톤 토큰 캐시 유지). 18종목 최악 sleep 30초/종목이지만 to_thread로 이벤트루프 영향 없음. systemctl restart (PID 242350→350159, KST 18:08). 5/11(월) 10:00 첫 자연 트리거에서 5/8 후보 19건 라벨링 100% 검증 예정 |
| 2026-05-07 | 종가베팅 universe_filters _maybe_run_per_ticker_fallback 헬퍼 분리 (closing-bet-unit-2-9-refactor, 단위 2-9 리팩토링) | before: `_maybe_run_per_ticker_fallback` 단일 함수 145줄(약 126 유효 라인)에 4개 책임 혼재 — 가드(bulk/tickers/캐시/cfg/토글) + 1순위 시총 보강(KIS market-cap top N) + 2순위 시총 보강(volume_rank lstn_stcn × stck_prpr) + OHLCV 보강(pykrx by_date). Phase 2 자동매매(2-4/2-5) 진입 전 정리 필요 — 자동매매 추가 시 fallback 함수 추가 비대화 회귀 위험 | after: `closing_bet_system/collectors/universe_filters.py` 헬퍼 3개 신규 — `_enrich_market_cap_from_kis_top(candidates, fallback, mcap_top_n, n_input) -> int` (단위 2-9d/2-9e KIS market-cap 매치, fallback dict in-place mutate) / `_enrich_market_cap_from_volume_rank(candidates, fallback) -> int` (단위 2-9f volume_rank 자체 계산, unmatched 자동 산출 + empty면 API 호출 스킵) / `_enrich_ohlcv_per_ticker(candidates, fallback, today_str, krx) -> tuple[int, float]` (단위 2-9c pykrx OHLCV + sleep 내장). 메인 함수는 가드 + candidates 빌드 + 헬퍼 3회 호출 + 캐시 저장만 수행 (raw 89줄, 본문 ~55줄). try/except 격리 의미 보존(1순위 실패→2순위 시도, 2순위 실패→옵션 A 보수). 두 시총 보강 모두 `cfg.fallback_include_market_cap=True` 블록 안에 중첩(토글 OFF 시 둘 다 스킵). | docs/work-plans/active/closing-bet-unit-2-9-refactor/PLAN.md | hatni | 동작 보존 리팩토링 (파라미터/전략/스키마 무변경). py_compile + import + 4개 가드 분기 단위 검증 PASS (bulk_result 비어있지 않음/tickers None/[]/캐시 히트). code-tester 심각 0건 / 주의 1건(`top_n=30` 하드코딩) — **리팩토링 이전과 동일값**(원본 line 529에서도 명시적 30) → 동작 보존 관점 정상, 후속 단위(`DEFAULT_VOLUME_RANK_TOP_N` 모듈 상수화 또는 settings cfg 노출)로 분리. 외부 호출자 없음(헬퍼 3개 모두 모듈 private `_` prefix), `_maybe_run_per_ticker_fallback` 시그니처 무변경. systemctl restart (PID 4045208→4070866, KST 21:18). 22개 스케줄 잡 정상 등록 확인. 자동매매 위험 0(Phase 1 알림형 유지). Phase 2 자동매매 진입 전 코드 정리 완료. 롤백: git revert 1회 즉시 복구 가능 |

<!--
예시 행 (실제 변경 시 이런 형식으로 추가):
| 2026-05-03 | STOP_LOSS_FAST | -0.07 | -0.06 | docs/improvements/2026-W17-weekly.md | hatni | 트레일링 사전 발동 빈도 감소 목적 |
-->

## 관련 링크
- 제안서 템플릿: `docs/improvements/_TEMPLATE.md`
- 에이전트 정의: `.claude/agents/trade-improvement-analyst.md`
- 관리 규칙: `docs/improvements/README.md`
