# TODO

## 1. 모니터링 포지션 로드 문제
- **현상**: 매수 체결 후 `start_monitoring()` → `portfolio_monitor_v2.load_positions_from_db()` → "모니터링할 포지션이 없습니다" (0개)
- **원인 추정**: `save_trade()`가 매매 기록만 저장하고, `get_portfolio()`가 조회하는 `holding` 상태 포지션이 별도로 생성되지 않음
- **해결 방향**:
  - `save_trade()` 호출 시 포트폴리오 테이블에 `holding` 상태로 포지션 추가
  - 또는 `load_positions_from_db()` 에서 당일 매수 기록도 포지션으로 인식하도록 수정
  - 모니터링이 정상 동작해야 분할 익절/트레일링 스탑/손절이 작동함

## 2. 신규 종목 선정 시 가중치별 리밸런싱
- **현상**: 다음날 새로운 종목이 선정되면 기존 보유 종목과의 가중치 조정 없이 추가 매수만 진행
- **필요 사항**:
  - 기존 보유 종목의 현재 평가금액 대비 비중 계산
  - 신규 종목 포함한 전체 포트폴리오 목표 가중치 재산정
  - 비중 초과 종목 일부 매도, 비중 부족 종목 추가 매수 (리밸런싱)
  - 기존 종목이 새 스크리닝에서 탈락 시 매도 처리
- **해결 방향**:
  - `execute_buy_orders()` 전에 기존 보유 현황 조회
  - `optimize_portfolio()`에 기존 포지션 정보 전달
  - 매수/매도 주문을 동시 생성하는 리밸런싱 로직 추가
