# 종가베팅 트레이딩 시스템 — 최종 PRD v2.0

> **문서 목적**: Claude Code 기반 바이브 코딩으로 단계적으로 구현 가능한 실전 운용 명세서  
> **버전**: v2.0 (v1.0 플랜 + 외부 LLM 개선안 + 비판적 검토 통합)  
> **작성일**: 2026-05-03  
> **운영 주체**: NOB Entertainment (개인 자금 분리 운용)

---

## 0. 변경 이력 및 핵심 변화

### v1.0 → v2.0 핵심 변화

| 구분 | v1.0 (원본 플랜) | v2.0 (최종) |
|---|---|---|
| 시스템 정체성 | 좋은 종목 추천기 | 비용 차감 후 EV 양수 상황만 허용하는 시스템 |
| 수급 데이터 | 장중 추정값을 진입 신호로 직접 사용 | 추정값/확정값 분리, 신뢰도 점수화 |
| 거래비용 | 명시적 반영 없음 | 매수/매도 수수료, 거래세, 슬리피지 모두 반영 |
| 후보 로그 | 매수 종목만 저장 | **추천+미진입+탈락 후보 전체 저장** |
| 진입 로직 | 동시호가 일괄 매수 | 정규장 마감 + 동시호가 2분할 |
| 매도 로직 | 갭업/갭다운 단순 분기 | 시가 상태 6단계별 액션 매트릭스 |
| 자동화 | Phase 3에서 통합 자동 | Phase 1 알림형 → Phase 4 부분 자동 (4단계) |
| 외부 리스크 | 미국 선물만 체크 | DART/KIND/미국선물/환율/이벤트 통합 |
| 검증 기준 | 4주 후 필터 조정 | **후보 100건 이상 누적 후 성능 평가** |

### 보류 또는 단순화한 항목

- **뉴스 감성 분석**: Phase 1~2에서 제외 (한국어 NLP 정확도 한계, false positive 우려). DART 공시 키워드 매칭으로 대체.
- **U-Shape 거래량 점수**: 가설 단계로 분류, 가중치 0으로 시작 → 100건 데이터 누적 후 가중치 학습.
- **동시호가 3분할(30/30/40)**: 동시호가는 단일가 체결이라 시간 분할이 무의미. 2분할(정규장 마감 + 동시호가)로 단순화.
- **호가 스프레드 0.4% 절대 기준**: 가격대별 호가단위 차이로 부적합. **20일 평균 스프레드의 1.5배 이내** 상대 기준으로 변경.
- **점수 가중치(수급 30점 등)**: Phase 1~2는 단순 조건 충족 개수로 시작 → Phase 3에서 로지스틱 회귀/LightGBM으로 학습.

### 추가된 항목

- MSCI / 코스피200 리밸런싱 별도 모듈 (수십 년 검증된 엣지)
- KIS 모의투자 환경 우선 검증 단계
- 시장 레짐 기록 시스템 (코스피 200일선, V-KOSPI, 외국인 누적)
- Kill Switch (자동 중단 안전장치)

---

## 1. 시스템 핵심 철학

### 1-1. 시스템 정체성

종가베팅 시스템 v2.0은 **"종목을 추천하는 도구"가 아니라 "기대값이 양수인 상황만 허락하는 도구"**다.

- 좋은 신호가 있어도 비용 차감 후 EV가 음수면 진입 금지
- 좋은 신호가 있어도 청산 유동성이 부족하면 진입 금지
- 좋은 신호가 있어도 외부 리스크(공시/거시) 노출 시 진입 금지
- 좋은 신호가 있어도 표본 100건 미만에서는 자동화 금지

### 1-2. 5대 핵심 원칙

| 번호 | 원칙 | 의미 |
|---|---|---|
| 1 | **EV-First** | 승률이 아니라 비용 차감 후 기대값으로 의사결정 |
| 2 | **Logging-First** | 매수/미매수 무관하게 모든 후보를 기록해야 시스템 개선 가능 |
| 3 | **Cost-Honest** | 거래비용/슬리피지/세금 모두 반영한 순수익만 인정 |
| 4 | **Risk-Capped** | 익일 오전 청산 가능성을 진입 시점에 사전 검증 |
| 5 | **Manual-First** | 알림형 → 반자동 → 부분 자동 단계적 자동화 |

---

## 2. 시스템 목표 및 KPI

### 2-1. 1차 목표 (Phase 1~2, 첫 8주)

| 항목 | 목표 |
|---|---|
| 후보 데이터 누적 | 100건 이상 |
| 시스템 안정성 | KIS API 에러율 1% 미만, 알림 누락 0건 |
| 수급 신뢰도 | 장중 추정값 vs 확정값 방향 일치율 측정 (베이스라인 확보) |
| 비용 정확도 | 백테스트 순수익 vs 실거래 순수익 차이 0.2%p 이내 |

### 2-2. 2차 목표 (Phase 3+, 8주 이후)

| 항목 | 목표 |
|---|---|
| 승률 | 60% 이상 (비용 차감 후) |
| 평균 수익 / 평균 손실 | 1.3배 이상 |
| 월간 최대 손실 | 종가베팅 자금의 -5% 이내 |
| 샤프 비율 (월 단위) | 1.0 이상 |

---

## 3. 전략 개요 및 운영 구조

### 3-1. 기본 전략

- **보유 기간**: 당일 14:30~15:30 매수 → 익일 09:00~10:30 매도 (약 16~18시간)
- **목표 수익**: 비용 차감 후 +1.0% ~ +2.5% (세전 +1.2% ~ +3.5%)
- **손절 기준**: 시가 -1% 이하 갭다운 시 즉시 청산
- **수익 원천**: 동시호가 수급 집중 + 오버나이트 호재 + 수급 연속성 + 세력의 종가 관리

### 3-2. 자금 운용 원칙

| 항목 | 기준 |
|---|---|
| 종가베팅 전용 자금 | 전체 자금의 10~15% (초기) → 성과 확인 후 점진 확대 |
| 1종목 최대 비중 | 종가베팅 전용 자금의 25% 이내 |
| 동시 보유 최대 | 4종목 (분산을 통한 개별 악재 리스크 분산) |
| 1일 최대 진입 | 2종목 (과매매 방지) |
| 주간 손실 한도 | 종가베팅 전용 자금 기준 -5% 도달 시 해당 주 매매 중지 |

---

## 4. 절대 제외 필터 (하드 룰)

스코어링 이전에 적용하는 **무조건 제외** 조건. 점수와 무관하게 진입 금지.

### 4-1. 종목 속성 기반 제외

- 관리종목, 투자경고, 투자위험, 거래정지 종목
- 보호예수 또는 의무보유 해제 D-7 이내 종목
- 당일 또는 D-1에 유상증자, CB/BW, 무상감자, 대규모 블록딜 공시 발생
- 당일 상한가 종목 (차익실현 압력 과도)
- 시가총액 500억 미만 (보조 필터, 거래대금이 주요 필터)
- 주가 1,000원 미만 (동전주 제외)
- 52주 고점 대비 -30% 초과 하락 종목

### 4-2. 시장 상황 기반 중지 (당일 전체 매매 중단)

- 익일 CPI, FOMC, 한국 금통위, 미국 고용지표 발표 예정
- 미국 선물 (S&P500/나스닥) 당일 -1.5% 이하 하락
- V-KOSPI 30 이상 (변동성 폭발 구간)

### 4-3. 시장 상황 기반 비중 축소 (50% 비중)

- 미국 선물 -1.0% ~ -1.5% 하락
- USD/KRW 당일 +1.5% 이상 급등
- 코스피 당일 -2% 이상 하락

### 4-4. 유동성 기반 제외

| 필터 | 기준 |
|---|---|
| 20일 평균 거래대금 | 50억 원 미만 시 제외 |
| 당일 거래대금 | 100억 원 미만 시 제외 |
| 호가 스프레드 | 20일 평균의 1.5배 초과 시 제외 |
| 주문금액 vs 유동성 | 내 주문금액이 최근 20분 거래대금의 3% 초과 시 제외 |

---

## 5. 4-Layer 스크리닝 체계 (수정판)

### Layer 1. 수급 지표 (최우선)

장중 KIS API 추정값과 KRX 확정값을 분리하여 다룬다.

| 지표 | 진입용 (장중) | 검증용 (사후) |
|---|---|---|
| 기관 추정 순매수 | 거래대금 대비 비율 (Z-score) | KRX 확정값과 방향 일치율 누적 |
| 외국인 추정 순매수 | 3일 누적 / 유동시총 | 확정값 대비 오차율 |
| 프로그램 순매수 | 15:00~15:20 변화율 | 확정값 검증 |
| 마감 수급 집중도 | 15:00 이후 순매수 / 당일 전체 순매수 | — |

**수급 신뢰도 점수**: 최근 20거래일 기준 종목별/섹터별 방향 일치율을 누적 기록. 70% 미만이면 가중치 감점.

### Layer 2. 가격 및 거래량 지표

| 지표 | 계산 | 기준 |
|---|---|---|
| Close Strength | (종가 - 저가) / (고가 - 저가) | 0.85 이상 가점, 0.92 이상 강가점 |
| 고점 이탈폭 | (고가 - 종가) / ATR | 0.4 이상이면 감점 |
| 마지막 30분 VWAP 위치 | 14:50~15:20 VWAP 대비 종가 | 종가 > VWAP일 때 가점 |
| 막판 체결 강도 | 14:50 이후 매수체결 / 매도체결 | 1.2 이상 가점, 0.9 미만 감점 |
| 거래량 서프라이즈 | 당일 거래량 / 20일 평균 | 2배 이상 가점, 3배 이상 강가점 |
| ATR 과열도 | 당일 상승폭 / ATR | 1.8 초과 시 제외 (구 +5% 룰 대체) |
| 가격 위치 | 20일 이동평균선 위/아래 | 위에 있을 때만 후보 인정 |

### Layer 3. 재료 및 모멘텀 지표

| 지표 | 기준 |
|---|---|
| 52주 신고가 | 신고가 또는 5% 이내 근접 시 가점 |
| 상대강도 (RS) | 코스피 대비 5일/20일 초과수익률 상위 30% |
| 테마 연동성 | 당일 테마 1~2일차 + 주도주 (테마 내 상승률 상위 3위) |
| 긍정 공시 | 당일 호재 공시 존재 시 가점 (수주, 실적, 신제품 등) |

### Layer 4. 외부 리스크 필터 (별도 모듈, 8장 참조)

DART 공시 / KIND 시장경보 / 미국 선물 / 환율 / 이벤트 캘린더를 별도 통합 모듈로 운영.

---

## 6. 점수 기반 스코어링 시스템

### 6-1. Phase 1~2: 단순 조건 충족 점수

**원칙**: 데이터가 쌓이기 전 가중치는 임의적이므로, 단순한 조건 카운트로 시작한다.

| 카테고리 | 만점 | 충족 조건 |
|---|---|---|
| 수급 (Layer 1) | 4점 | 기관 순매수 / 외국인 3일 연속 / 프로그램 매수 전환 / 마감 수급 집중 (각 1점) |
| 가격 (Layer 2) | 4점 | Close Strength 0.85+ / 거래량 2배+ / 20일선 위 / 막판 체결강도 1.2+ (각 1점) |
| 모멘텀 (Layer 3) | 3점 | 52주 신고가 근접 / 테마 주도주 / 긍정 공시 (각 1점) |
| 합계 | **11점** | — |

**진입 기준 (Phase 1~2)**:
- 7점 이상: 알림 후보
- 8점 이상: 진입 가능
- 9점 이상 + 시장 양호: 최대 비중 허용

### 6-2. Phase 3+: 학습 기반 가중치

후보 100건 이상 누적 후, 다음 절차로 가중치를 학습한다.

1. 라벨: 익일 09:30 최고가 / 진입가 - 1 ≥ 0.012 (성공)
2. 피처: 위 11개 지표 + 수급 신뢰도 + 변동성 등 보조 지표
3. 모델: 로지스틱 회귀 (해석 가능) → LightGBM (성능)
4. 검증: Walk-forward 방식, 시계열 분할
5. 운영: 모델 출력 확률 + EV 계산 → 진입 결정

---

## 7. 거래비용 및 슬리피지 엔진

### 7-1. 한국 주식 거래비용 구조

| 항목 | 코스피 | 코스닥 | 비고 |
|---|---|---|---|
| 매수 수수료 | 0.015% (증권사별) | 0.015% | 키움/한투 기준 |
| 매도 수수료 | 0.015% | 0.015% | — |
| 거래세 (매도) | 0.18% | 0.18% (농특세 0.15% 포함) | 2026 기준 |
| 호가 스프레드 (편도) | 0.05~0.20% | 0.05~0.30% | 종목별 |
| 슬리피지 (편도) | 0.05~0.15% | 0.10~0.25% | 유동성별 |
| **왕복 총비용** | **0.4~0.6%** | **0.4~0.7%** | — |

### 7-2. 순수익률 계산식

```
실거래 순수익률 = ((매도가 × (1 - 매도수수료 - 거래세)) - (매수가 × (1 + 매수수수료))) / (매수가 × (1 + 매수수수료))

목표 수익률 (세전) = 거래비용 + 슬리피지 + 안전마진 0.5%
                  = 약 +1.2% (코스피 기준 최소)
                  = 약 +1.4% (코스닥 기준 최소)
```

### 7-3. 모듈 책임

- 백테스트 시: **단순 종가/시가가 아닌 예상체결가 + 호가 스프레드 + 추정 슬리피지** 반영
- 실거래 시: 매수가, 매도가, 수수료, 세금, 슬리피지 모두 로깅
- 모든 성과 표시는 **세후 순수익률** 기준

---

## 8. 외부 리스크 필터 모듈

### 8-1. 데이터 소스 및 활용

| 소스 | 무료 여부 | 활용 |
|---|---|---|
| **DART OpenAPI** | 무료 | 장 마감 후 공시 실시간 모니터링 (15:30 이후) |
| **KIND** | 무료 | 거래정지, 불성실공시, 시장경보 |
| **KRX 정보데이터시스템** | 무료 | 확정 수급, 공매도 잔고 (T+1) |
| **예탁결제원 SEIBro** | 무료 | 보호예수 해제 일정 |
| **미국 선물 (CME)** | 야후/인베스팅 크롤링 또는 KIS API | 나스닥/S&P500 |
| **USD/KRW** | KIS API | 환율 |
| **이벤트 캘린더** | 인베스팅닷컴 또는 econdb | CPI/FOMC/금통위 등 |

### 8-2. DART 공시 키워드 분류 (감점/제외 매트릭스)

| 키워드 | 액션 | 비고 |
|---|---|---|
| 유상증자 결정 | 즉시 제외 | 익일 -5% 이상 갭다운 빈번 |
| CB/BW 발행 | 즉시 제외 | 동일 |
| 무상감자 | 즉시 제외 | — |
| 횡령/배임 혐의 | 즉시 제외 | — |
| 영업정지/제재 | 즉시 제외 | — |
| 단일판매공급계약 (수주) | 가점 +1 | 호재 |
| 자기주식 취득 결정 | 가점 +1 | 호재 |
| 실적 공시 (양호) | 가점 +1 | 컨센서스 비교 필요 |
| 실적 공시 (쇼크) | 즉시 제외 | 컨센서스 -10% 이하 |

**구현 방식**: Phase 1은 키워드 매칭 (정규표현식). Phase 3 이후 LLM 분류기로 고도화.

### 8-3. 외부 리스크 종합 판단

```
외부 리스크 점수 = -(DART 악재 감점) - (KIND 경보 감점) - (미국 선물 감점) - (환율 감점)
                  + (DART 호재 가점)

총 점수 = 스크리닝 점수 + 외부 리스크 점수
```

---

## 9. 동시호가 분할 진입 로직 (단순화)

### 9-1. 2분할 진입

| 시간 | 행동 | 비중 | 주문 타입 |
|---|---|---|---|
| 15:18~15:19 | 정규장 마지막 유동성에서 1차 진입 | 50% | 지정가 |
| 15:25~15:28 | 예상체결가 확인 후 동시호가 2차 진입 | 50% | 동시호가 지정가 |

### 9-2. 가격 제한 룰

```
매수 허용 상한가 = min(
    14:50~15:18 VWAP × 1.005,
    당일 고가,
    예상체결가 × 1.002 (15:20 이후)
)
```

허용가 초과 시 추격 매수하지 않는다. 1차에서 체결 안 되면 2차도 포기하고 다음 후보로 넘어간다.

### 9-3. 동시호가 예상체결가 모니터링

- 15:20부터 1초 간격으로 KIS API로 예상체결가/예상거래량 폴링
- 예상체결가가 1차 매수가 대비 +0.5% 이상 상승 → 2차 진입 보류
- 호가 매수잔량/매도잔량 비율 0.8 미만으로 떨어지면 2차 진입 취소

---

## 10. 익일 매도 로직

### 10-1. 시가 상태별 액션 매트릭스

| 시가 상황 | 1차 액션 | 2차 액션 |
|---|---|---|
| 시가 +2% 이상 갭업 | 09:00 즉시 50~70% 매도 | 잔여 물량 09:10 전후 고점 추적 후 청산 |
| 시가 +0.5% ~ +2% | 09:03~09:15 체결강도/거래대금 확인 | 추가 상승 시 분할매도, 약화 시 전량 |
| 시가 보합 (-0.5% ~ +0.5%) | 09:00~09:10 추이 관찰 | 09:10까지 고가 돌파 실패 시 전량 매도 |
| 시가 -0.5% ~ -1% 약갭다운 | 09:05까지 회복 시도 관찰 | 회복 실패 시 09:10 전량 매도 |
| 시가 -1% 이하 갭다운 | **개장 직후 즉시 전량 손절** | — |
| 갭업 후 5분봉 음봉 장악 | **즉시 분할매도** (수익 반납 방지) | — |
| 시가 강세 + 거래대금 폭발 | 트레일링 스탑 적용 (-1.5% trailing) | 10:30 이전 청산 원칙 |

### 10-2. 운영 원칙

- 익일 오전에는 **추가 수익보다 리스크 회수 우선**
- 종가베팅은 "맞췄을 때 빨리 현금화하는 전략"
- 09:30 이후 보유는 원칙적으로 비권장 (스윙으로 전환할 명확한 사유 없는 한)
- 10:30까지 청산 미완료 시 무조건 시장가 매도

### 10-3. 미국 시장 야간 변동 대응

| 미국 야간 상황 | 대응 |
|---|---|
| S&P500 / 나스닥 -1.5% 이상 급락 | 익일 시가 무조건 매도 (가격 무관) |
| 보유 종목 관련 해외 악재 발생 | 시가 무관 오전 첫 강세 구간에 청산 |
| 한국 야간 선물 -1% 이상 하락 | 시가 약갭다운 시에도 즉시 청산 우선 |

---

## 11. 후보 전체 로그 시스템

**시스템의 가장 중요한 자산**. 매수한 종목만 저장하면 필터의 진짜 성능을 측정할 수 없다.

### 11-1. SQLite 스키마 (핵심 테이블)

```sql
-- 후보 종목 마스터
CREATE TABLE candidates (
    candidate_id INTEGER PRIMARY KEY AUTOINCREMENT,
    trade_date DATE NOT NULL,
    ticker TEXT NOT NULL,
    name TEXT NOT NULL,
    candidate_status TEXT NOT NULL,  -- 'recommended', 'entered', 'rejected_filter', 'rejected_manual'
    rejection_reason TEXT,            -- 탈락 사유 (필터명 또는 사용자 판단)
    
    -- 점수
    layer1_score INTEGER,
    layer2_score INTEGER,
    layer3_score INTEGER,
    external_risk_score INTEGER,
    total_score INTEGER,
    
    -- 진입 정보 (entered만)
    entry_price REAL,
    entry_amount REAL,
    entry_time TIMESTAMP,
    
    -- 청산 정보 (entered만)
    exit_price REAL,
    exit_time TIMESTAMP,
    
    -- 비용 (entered만)
    buy_commission REAL,
    sell_commission REAL,
    transaction_tax REAL,
    estimated_slippage REAL,
    net_pnl_pct REAL,
    
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 진입 시점 피처 스냅샷 (모든 후보)
CREATE TABLE candidate_features (
    candidate_id INTEGER PRIMARY KEY,
    
    -- Layer 1 수급
    inst_net_buy_estimated REAL,
    foreign_net_buy_3d REAL,
    program_net_buy_change REAL,
    closing_flow_concentration REAL,
    
    -- Layer 2 가격/거래량
    close_strength REAL,
    upper_shadow_atr REAL,
    last_30min_vwap_position REAL,
    closing_buy_sell_ratio REAL,
    volume_surprise REAL,
    atr_overheat REAL,
    
    -- Layer 3 모멘텀
    days_from_52w_high INTEGER,
    relative_strength_5d REAL,
    theme_leadership_rank INTEGER,
    
    -- 시장 레짐
    kospi_above_200ma BOOLEAN,
    vkospi REAL,
    foreign_5d_cumulative REAL,
    us_futures_change REAL,
    usd_krw_change REAL,
    
    FOREIGN KEY (candidate_id) REFERENCES candidates(candidate_id)
);

-- 사후 라벨 (T+1 09:30 데이터로 채움)
CREATE TABLE candidate_labels (
    candidate_id INTEGER PRIMARY KEY,
    
    next_open_pct REAL,              -- 익일 시가 / 진입가 - 1
    next_morning_high_pct REAL,      -- 09:00~09:30 고가 / 진입가 - 1
    next_morning_low_pct REAL,       -- 09:00~09:30 저가 / 진입가 - 1
    
    label_gap_up BOOLEAN,            -- next_open_pct >= 0.006
    label_morning_exit BOOLEAN,      -- next_morning_high_pct >= 0.012
    label_stop_risk BOOLEAN,         -- next_morning_low_pct <= -0.010
    label_net_ev_positive BOOLEAN,   -- 비용 차감 후 EV > 0
    
    FOREIGN KEY (candidate_id) REFERENCES candidates(candidate_id)
);

-- 수급 신뢰도 추적
CREATE TABLE flow_data_reliability (
    record_id INTEGER PRIMARY KEY AUTOINCREMENT,
    trade_date DATE NOT NULL,
    ticker TEXT NOT NULL,
    
    inst_estimated REAL,             -- 장중 추정
    inst_confirmed REAL,             -- KRX 확정 (T+1)
    foreign_estimated REAL,
    foreign_confirmed REAL,
    
    inst_direction_match BOOLEAN,    -- 부호 일치 여부
    foreign_direction_match BOOLEAN,
    
    UNIQUE(trade_date, ticker)
);
```

### 11-2. 로깅 원칙

- **추천 후보**: 알림 발송 시점에 저장 (status: 'recommended')
- **진입 후보**: 매수 체결 시 entry_* 필드 업데이트 (status: 'entered')
- **수동 미진입**: 알림 받았지만 진입 안 한 경우 (status: 'rejected_manual')
- **필터 탈락**: 점수 미달 또는 하드 필터 위반 (status: 'rejected_filter')
- **사후 라벨**: T+1 10:00 자동 배치로 candidate_labels 채움

---

## 12. EV 기반 의사결정

### 12-1. 라벨 정의

| 라벨 | 계산 | 성공 기준 |
|---|---|---|
| Gap-up | 익일 시가 / 진입가 - 1 | ≥ +0.6% |
| Morning Exit | 익일 09:00~09:30 고가 / 진입가 - 1 | ≥ +1.2% |
| Stop Risk | 익일 09:00~09:30 저가 / 진입가 - 1 | ≤ -1.0% |

### 12-2. EV 계산식

```
EV = P(Morning Exit 도달) × 평균 익절 수익률
   - P(Stop Risk 도달) × 평균 손실률
   - 거래비용 (왕복 약 0.5%)
   - 슬리피지 (왕복 약 0.2%)

진입 허용 조건: EV > 0
강한 진입: EV > 0.5%
```

### 12-3. EV 추정 방식

- **Phase 1~2**: 충분한 데이터가 없으므로 EV 계산은 사후 검증용으로만 사용
- **Phase 3+**: 100건 이상 누적 후, 점수 구간별 P(Morning Exit) / P(Stop Risk) 추정 → 진입 결정에 직접 사용

---

## 13. MSCI / 코스피200 리밸런싱 모듈

**별도 모듈로 운영**. 이 신호가 발생한 날은 일반 종가베팅보다 우선순위 높음.

### 13-1. 정기변경 일정

| 인덱스 | 정기변경 | 발표 시점 |
|---|---|---|
| MSCI Korea | 2월/5월/8월/11월 마지막 영업일 적용 | 적용 약 2주 전 발표 |
| 코스피 200 | 6월/12월 정기변경 | 약 한 달 전 발표 |
| 코스닥 150 | 6월/12월 정기변경 | 약 한 달 전 발표 |

### 13-2. 운영 로직

1. 발표 후 편입 예정 종목 리스트 자동 수집 (KRX 공시)
2. 정기변경 D-Day까지 매일 종가베팅 후보로 추가
3. 일반 후보보다 점수 +2점 가산 (구조적 패시브 매수 확정)
4. 외국인/기관 순매수가 동반되는 날 집중 진입
5. 정기변경 당일 종가에 청산 (편입 효과 종료)

### 13-3. 별도 자금 풀

- 종가베팅 전용 자금 중 최대 30%까지만 리밸런싱 모듈에 할당
- 일반 종가베팅과 동시 진입 가능 (전략 분리)

---

## 14. 시장 레짐 기록 시스템

자동화 로직은 아니지만, **모든 후보 진입 시점의 시장 레짐을 기록**하여 사후 분석에 활용.

### 14-1. 3대 레짐 지표

| 지표 | 분류 |
|---|---|
| 코스피 200일선 | 위 / 아래 |
| V-KOSPI | <15 / 15~25 / >25 |
| 외국인 5일 누적 매매 | 순매수 / 순매도 |

### 14-2. 활용 방식

- **Phase 1~2**: candidate_features에 레짐 정보 기록
- **Phase 3+**: 레짐별 승률/EV 분석 → 특정 레짐에서만 진입 또는 비중 차등화

예시 가설 (사후 검증 대상):
- 코스피 200일선 위 + 외국인 순매수 → EV 높음 (정상 베팅)
- 코스피 200일선 아래 + V-KOSPI 25 이상 → EV 음수 (전체 중지)

---

## 15. 기존 스윙 시스템과의 리스크 통합

전략별 자금 풀은 분리하되, **동일 종목/테마 노출은 통합 관리**.

| 관리 항목 | 기준 |
|---|---|
| 동일 종목 총 노출 | 스윙 + 종가베팅 합산 비중 관리 (전체 자금의 5% 이내) |
| 동일 테마 노출 | 하루 동일 테마 2종목 이상 진입 금지 |
| 중복 진입 우선순위 | 스윙 보유 종목은 종가베팅 후보에서 제외 |
| 정보 공유 | 스윙 시스템 DB와 종가베팅 DB 간 보유 종목 조회 API 구축 |

---

## 16. 시스템 아키텍처

### 16-1. 기술 스택

| 컴포넌트 | 기술 | 비고 |
|---|---|---|
| 데이터 수집 | KIS API (한국투자증권) | 수급, 호가, 예상체결, 거래량 |
| 외부 데이터 | DART OpenAPI, KIND, KRX, SEIBro | REST API 또는 크롤링 |
| 서버 환경 | GCP VM (Ubuntu 22.04) | 기존 스윙 시스템과 동일 인프라 |
| 언어/프레임워크 | Python 3.11 + FastAPI | 비동기 처리 권장 |
| 데이터베이스 | SQLite | 단일 사용자 환경에 충분 |
| 알림 | Telegram Bot | 인터랙티브 버튼 (Phase 2+) |
| 스케줄러 | cron | 시간 정밀도 1초 |
| 개발 방식 | Claude Code (바이브 코딩) | CLAUDE.md 기반 |
| 모의투자 | KIS 모의투자 환경 | Phase 1에서 우선 검증 |

### 16-2. 모듈 구성

```
closing_bet_system/
├── collectors/
│   ├── kis_intraday_flow_collector.py    # 장중 수급 스냅샷
│   ├── kis_price_volume_collector.py     # 가격/거래량
│   ├── kis_orderbook_collector.py        # 호가/예상체결
│   ├── dart_disclosure_collector.py      # DART 공시
│   ├── kind_alert_collector.py           # KIND 시장경보
│   └── external_data_collector.py        # 미국선물/환율/이벤트
├── engines/
│   ├── signal_score_engine.py            # 스코어링 (Phase 1~2 단순 / Phase 3+ ML)
│   ├── cost_slippage_engine.py           # 비용/슬리피지 계산
│   ├── overnight_risk_filter.py          # 외부 리스크 통합
│   ├── ev_calculator.py                  # EV 계산 (Phase 3+)
│   └── regime_detector.py                # 시장 레짐
├── execution/
│   ├── entry_executor.py                 # 2분할 진입 (Phase 2+)
│   ├── morning_exit_manager.py           # 익일 매도
│   └── kill_switch.py                    # 안전장치
├── modules/
│   ├── msci_rebalancing_module.py        # 별도 모듈
│   └── swing_integration.py              # 스윙 시스템 연계
├── storage/
│   ├── candidate_logger.py               # SQLite 로깅
│   └── flow_reliability_tracker.py       # 수급 신뢰도
├── notification/
│   ├── telegram_review_bot.py            # 알림형 (Phase 1)
│   └── telegram_interactive_bot.py       # 인터랙티브 (Phase 2+)
├── dashboard/
│   └── dashboard_fastapi.py              # 성과/필터 기여도
├── config/
│   └── settings.yaml                     # 전체 파라미터
└── main_orchestrator.py                  # 시간별 실행 흐름
```

### 16-3. 일별 실행 흐름

| 시간 | 단계 | 작업 |
|---|---|---|
| 13:30 | 사전 체크 | 미국 선물, 환율, 익일 이벤트, 시장 레짐 기록 |
| 14:00 | Layer 3 추출 | 테마/모멘텀 후보 수집 |
| 14:30 | Layer 1 1차 | 수급 우위 종목 추출 |
| 15:00 | Layer 1+2 | 수급 + 가격/거래량 통합 스크리닝 |
| 15:10 | 외부 리스크 | DART/KIND/미국선물 통합 필터 |
| 15:15 | 점수 산출 + 알림 | Telegram 발송 (Phase 1: 정보만 / Phase 2+: 확인 버튼) |
| 15:18~15:19 | 1차 진입 | Phase 1: 수동 / Phase 2+: 반자동 |
| 15:25~15:28 | 2차 진입 | 동시호가 예상체결가 검증 후 |
| 15:30 | 마감 후 처리 | 후보 로그 확정, 진입 정보 저장 |
| 15:35~16:00 | DART 모니터링 | 장 마감 후 공시 확인 (악재 시 익일 시가 청산 알림) |
| 22:00 | 미국장 모니터링 | 야간 급락 알림 |
| T+1 08:50 | 매도 준비 | 시가 예측, 매도 액션 알림 |
| T+1 09:00~10:30 | 매도 실행 | 시가 상황별 액션 매트릭스 |
| T+1 10:00 | 사후 처리 | KRX 확정 수급 수집, 라벨링, 신뢰도 업데이트 |

---

## 17. 구현 로드맵 (4 Phase)

### Phase 1. 알림형 검증 (2~4주)

**목표**: 데이터 수집 인프라 + 후보 로그 시스템 + 알림 운영. **자동매수 절대 금지**.

- [ ] KIS API 연동 (모의투자 환경 우선)
- [ ] kis_intraday_flow_collector 구현
- [ ] kis_price_volume_collector 구현
- [ ] candidate_logger SQLite 스키마 구축
- [ ] signal_score_engine (Phase 1 단순 점수 버전)
- [ ] cost_slippage_engine 기본 구현
- [ ] overnight_risk_filter (DART + 미국선물만)
- [ ] telegram_review_bot (정보 전달만)
- [ ] 모의투자에서 1주, 실전 소액 (50~100만원)으로 1~2주 검증

**완료 기준**: 후보 로그 50건 이상 누적, 알림 시스템 안정 동작.

### Phase 2. 반자동 주문 (4~8주)

**목표**: 점수 기반 후보 선별 + 확인 버튼 후 주문 + 익일 매도 자동 알림.

- [ ] telegram_interactive_bot (확인 버튼 추가)
- [ ] entry_executor (2분할 지정가 주문, 사용자 승인 기반)
- [ ] morning_exit_manager (시가 상황별 매도 알림)
- [ ] kis_orderbook_collector (호가/예상체결)
- [ ] kind_alert_collector
- [ ] dashboard_fastapi 기본 버전 (후보 통계, 필터 기여도)
- [ ] flow_reliability_tracker (수급 신뢰도 누적)

**완료 기준**: 후보 100건 이상, 비용 차감 후 평균 EV 측정 가능.

### Phase 3. 부분 자동화 (3~6개월)

**목표**: EV 양수 조건 자동 진입 + 시장 레짐 기반 비중 조절.

- [ ] ev_calculator 본격 가동
- [ ] signal_score_engine을 ML 기반으로 전환 (로지스틱 회귀)
- [ ] regime_detector + 레짐별 비중 조절
- [ ] msci_rebalancing_module
- [ ] swing_integration (동일 종목/테마 통합 관리)
- [ ] kill_switch 구현 및 가동
- [ ] 주간 손실 시 자동 중지 로직

**완료 기준**: 3개월 이상 실거래 데이터, 비용 차감 후 양의 수익 확인.

### Phase 4. 운영 최적화 (6개월 이후)

**목표**: ML 모델 고도화 + 필터 자동 학습.

- [ ] LightGBM 기반 스코어링
- [ ] 종목/섹터별 신뢰도 자동 조정
- [ ] U-Shape 점수 등 보조 피처 가중치 학습
- [ ] 뉴스 감성 분석 (검증된 한국어 모델 또는 LLM 분류기)
- [ ] 백테스트 자동화 + Walk-forward 검증

---

## 18. 안전장치 (Kill Switch)

### 18-1. 자동 중단 트리거

| 조건 | 액션 |
|---|---|
| KIS API 응답 시간 5초 이상 | 당일 추가 진입 중단 |
| KIS API 에러율 5% 이상 (10분 윈도우) | 즉시 모든 신규 주문 중단 |
| 1시간 안에 보유 종목 3개 이상 손절 신호 | 즉시 중단, 사용자 알림 |
| 일일 평가손실 -3% 도달 | 당일 추가 진입 금지 |
| 주간 평가손실 -5% 도달 | 해당 주 매매 중지 |
| Telegram bot 30분 이상 무응답 | 시스템 자체 중단 + SMS 알림 |

### 18-2. 수동 Kill Switch

- Telegram에 `/killswitch` 명령어 → 즉시 모든 자동 주문 중단
- 보유 포지션은 시장가 청산 또는 수동 처리 선택 가능

---

## 19. 구현 태스크 목록 (우선순위)

| 우선순위 | 모듈 | Phase | 설명 |
|---|---|---|---|
| 최상 | candidate_logger.py | 1 | SQLite 로깅 — 모든 시스템의 기반 |
| 최상 | kis_intraday_flow_collector.py | 1 | 15:00/15:10/15:20/15:28 스냅샷 |
| 최상 | cost_slippage_engine.py | 1 | 거래세/수수료/스프레드/슬리피지 |
| 최상 | overnight_risk_filter.py (기본) | 1 | DART + 미국선물 |
| 최상 | telegram_review_bot.py | 1 | 알림 전달 |
| 상 | signal_score_engine.py (단순) | 1 | 조건 충족 점수 |
| 상 | morning_exit_manager.py | 2 | 익일 매도 알림 |
| 상 | entry_executor.py | 2 | 2분할 진입 |
| 상 | dashboard_fastapi.py | 2 | 성과/필터 기여도 |
| 상 | flow_reliability_tracker.py | 2 | 수급 신뢰도 |
| 중 | ev_calculator.py | 3 | EV 계산 |
| 중 | regime_detector.py | 3 | 시장 레짐 |
| 중 | msci_rebalancing_module.py | 3 | 별도 모듈 |
| 중 | kill_switch.py | 3 | 안전장치 |
| 중 | swing_integration.py | 3 | 스윙 연계 |
| 하 | LightGBM 스코어링 | 4 | 가중치 학습 |
| 하 | 뉴스 감성 분석 | 4 | LLM 분류기 |

---

## 20. 운영 원칙 체크리스트

### 진입 시점

- [ ] 하루 최대 2종목만 진입한다
- [ ] 종가베팅 전용 자금은 초기 10~15% 이내로 시작한다
- [ ] 동일 테마 2종목 이상 진입하지 않는다
- [ ] 시총보다 거래대금/호가 두께/스프레드를 우선 검증한다
- [ ] 매수 허용 상한가를 넘으면 추격 매수하지 않는다

### 데이터 운용

- [ ] 장중 추정 수급은 신호로만 쓰고, 확정 수급은 사후 검증용으로 쓴다
- [ ] 모든 수익률은 세금/수수료/슬리피지 포함 순수익률로 본다
- [ ] 추천 후보, 미진입 후보, 탈락 후보를 모두 저장한다
- [ ] 후보 100건 전까지 전략 성능을 확정 평가하지 않는다

### 리스크

- [ ] 익일 오전 매도는 욕심보다 리스크 회수를 우선한다
- [ ] CPI/FOMC/금통위/미국 고용지표 전일은 원칙적으로 쉰다
- [ ] 미국 선물 -1.5% 이하면 당일 전체 매매 중지
- [ ] 자동매수는 후보 100건 + 3개월 검증 이후에만 허용한다

### 시스템

- [ ] 모의투자 환경에서 1주 이상 검증 후 실전 진입한다
- [ ] Kill Switch 트리거 조건을 매주 점검한다
- [ ] DART 공시 모니터링은 장 마감 후 30분 이상 유지한다

---

## 21. 한 줄 결론

> **이 시스템의 목표는 "다음날 오를 종목을 맞추는 것"이 아니라 "비용 차감 후 기대값이 양수인 상황에서만, 익일 오전 안에 청산할 수 있는 포지션만 잡는 것"이다.**  
>  
> 후보 전체 로그, 거래비용 반영, 외부 리스크 필터, 익일 오전 매도 규칙, 단계적 자동화 — 이 다섯 가지가 들어가야 "종목 추천기"가 아니라 "돈이 새지 않는 단기 트레이딩 시스템"이 된다.

---

**문서 끝** | 종가베팅 트레이딩 시스템 PRD v2.0 | NOB Entertainment | 2026-05-03
